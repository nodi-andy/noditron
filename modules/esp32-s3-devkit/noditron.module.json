{
  "noditronModule": 1,
  "name": "esp32-s3-devkit",
  "displayName": "esp32-S3",
  "version": "1.8.0",
  "description": "Waveshare ESP32-S3-POE-ETH-8DI-8DO: DI1-DI8, DO1-D8, direct CAN In/Out with configurable speed, and USB firmware installation.",
  "swatchColor": "#2f6fed",
  "block": {
    "format": "nodigraph/clipboard-v1",
    "blocks": [
      {
        "id": "blk_mtg8v3eu8",
        "name": "esp32-S3",
        "type": "block",
        "kind": "block",
        "description": "",
        "geometry": {
          "x": 0,
          "y": 0,
          "width": 300,
          "height": 540
        },
        "style": {
          "color": "#3b6fa0"
        },
        "logicalPorts": [
          {
            "id": "io_esp32s3_left_di1_1",
            "name": "DI1",
            "direction": "in",
            "description": "GPIO4"
          },
          {
            "id": "io_esp32s3_left_di2_1",
            "name": "DI2",
            "direction": "in",
            "description": "GPIO5"
          },
          {
            "id": "io_esp32s3_left_di3_1",
            "name": "DI3",
            "direction": "in",
            "description": "GPIO6"
          },
          {
            "id": "io_esp32s3_left_di4_1",
            "name": "DI4",
            "direction": "in",
            "description": "GPIO7"
          },
          {
            "id": "io_esp32s3_left_di5_1",
            "name": "DI5",
            "direction": "in",
            "description": "GPIO8"
          },
          {
            "id": "io_esp32s3_left_di6_1",
            "name": "DI6",
            "direction": "in",
            "description": "GPIO9"
          },
          {
            "id": "io_esp32s3_left_di7_1",
            "name": "DI7",
            "direction": "in",
            "description": "GPIO10"
          },
          {
            "id": "io_esp32s3_left_di8_1",
            "name": "DI8",
            "direction": "in",
            "description": "GPIO11"
          },
          {
            "id": "io_esp32s3_left_canin_1",
            "name": "CAN In",
            "direction": "in",
            "description": "GPIO3"
          },
          {
            "id": "io_esp32s3_right_do1_1",
            "name": "DO1",
            "direction": "out",
            "description": "GPIO1000 — TCA9554 EXIO1 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do2_1",
            "name": "DO2",
            "direction": "out",
            "description": "GPIO1001 — TCA9554 EXIO2 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do3_1",
            "name": "DO3",
            "direction": "out",
            "description": "GPIO1002 — TCA9554 EXIO3 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do4_1",
            "name": "DO4",
            "direction": "out",
            "description": "GPIO1003 — TCA9554 EXIO4 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do5_1",
            "name": "DO5",
            "direction": "out",
            "description": "GPIO1004 — TCA9554 EXIO5 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do6_1",
            "name": "DO6",
            "direction": "out",
            "description": "GPIO1005 — TCA9554 EXIO6 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do7_1",
            "name": "DO7",
            "direction": "out",
            "description": "GPIO1006 — TCA9554 EXIO7 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_do8_1",
            "name": "DO8",
            "direction": "out",
            "description": "GPIO1007 — TCA9554 EXIO8 / isolated Darlington output"
          },
          {
            "id": "io_esp32s3_right_canout_1",
            "name": "CAN Out",
            "direction": "out",
            "description": "GPIO2"
          },
          {
            "id": "io_esp32s3_right_canspeed_1",
            "name": "CAN speed",
            "direction": "out",
            "description": "can-speed"
          },
          {
            "id": "io_esp32s3_right_rs485tx_1",
            "name": "RS485 TX",
            "direction": null,
            "description": "GPIO17 — unavailable: RS485 TX"
          },
          {
            "id": "io_esp32s3_right_rs485rx_1",
            "name": "RS485 RX",
            "direction": null,
            "description": "GPIO18 — unavailable: RS485 RX"
          },
          {
            "id": "io_esp32s3_top_usb_1",
            "name": "USB",
            "direction": null,
            "description": "usb-serial"
          }
        ],
        "ports": [
          {
            "id": "prt_esp32s3_left_di1_1",
            "logicalId": "io_esp32s3_left_di1_1",
            "side": "left",
            "offset": 30,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di2_1",
            "logicalId": "io_esp32s3_left_di2_1",
            "side": "left",
            "offset": 70,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di3_1",
            "logicalId": "io_esp32s3_left_di3_1",
            "side": "left",
            "offset": 110,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di4_1",
            "logicalId": "io_esp32s3_left_di4_1",
            "side": "left",
            "offset": 150,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di5_1",
            "logicalId": "io_esp32s3_left_di5_1",
            "side": "left",
            "offset": 190,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di6_1",
            "logicalId": "io_esp32s3_left_di6_1",
            "side": "left",
            "offset": 230,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di7_1",
            "logicalId": "io_esp32s3_left_di7_1",
            "side": "left",
            "offset": 270,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_di8_1",
            "logicalId": "io_esp32s3_left_di8_1",
            "side": "left",
            "offset": 310,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_left_canin_1",
            "logicalId": "io_esp32s3_left_canin_1",
            "side": "left",
            "offset": 350,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do1_1",
            "logicalId": "io_esp32s3_right_do1_1",
            "side": "right",
            "offset": 30,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do2_1",
            "logicalId": "io_esp32s3_right_do2_1",
            "side": "right",
            "offset": 70,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do3_1",
            "logicalId": "io_esp32s3_right_do3_1",
            "side": "right",
            "offset": 110,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do4_1",
            "logicalId": "io_esp32s3_right_do4_1",
            "side": "right",
            "offset": 150,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do5_1",
            "logicalId": "io_esp32s3_right_do5_1",
            "side": "right",
            "offset": 190,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do6_1",
            "logicalId": "io_esp32s3_right_do6_1",
            "side": "right",
            "offset": 230,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do7_1",
            "logicalId": "io_esp32s3_right_do7_1",
            "side": "right",
            "offset": 270,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_do8_1",
            "logicalId": "io_esp32s3_right_do8_1",
            "side": "right",
            "offset": 310,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_canout_1",
            "logicalId": "io_esp32s3_right_canout_1",
            "side": "right",
            "offset": 350,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_canspeed_1",
            "logicalId": "io_esp32s3_right_canspeed_1",
            "side": "right",
            "offset": 390,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_rs485tx_1",
            "logicalId": "io_esp32s3_right_rs485tx_1",
            "side": "right",
            "offset": 430,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_right_rs485rx_1",
            "logicalId": "io_esp32s3_right_rs485rx_1",
            "side": "right",
            "offset": 470,
            "manualOffset": true
          },
          {
            "id": "prt_esp32s3_top_usb_1",
            "logicalId": "io_esp32s3_top_usb_1",
            "side": "top",
            "offset": 150,
            "manualOffset": true
          }
        ],
        "props": [
          {
            "id": "prp_mtg8v3eu9",
            "name": "noditronKind",
            "kind": "value",
            "value": "esp32-devkit"
          },
          {
            "id": "prp_mtg8v3eua",
            "name": "chipFamily",
            "kind": "value",
            "value": "ESP32-S3"
          },
          {
            "id": "prp_mtg8v3eub",
            "name": "connectionState",
            "kind": "value",
            "value": "disconnected"
          },
          {
            "id": "prp_mtg8v3euc",
            "name": "lastSentSnapshot",
            "kind": "value",
            "value": ""
          },
          {
            "id": "prp_mtg8v3eud",
            "name": "html",
            "kind": "value",
            "value": "container.style.position = 'absolute';\nlet pill = container.querySelector('.esp-status');\nif (!pill) {\n  container.replaceChildren();\n  pill = document.createElement('button');\n  pill.type = 'button';\n  pill.className = 'esp-status';\n  pill.setAttribute('aria-label', 'Connection settings');\n  pill.innerHTML = '<span class=\"esp-status-dot\"></span><span class=\"esp-status-text\"></span>'\n    + '<svg class=\"esp-status-gear\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\">'\n    + '<circle cx=\"12\" cy=\"12\" r=\"3\"></circle>'\n    + '<path d=\"M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z\"></path>'\n    + '</svg>';\n  pill.addEventListener('click', helpers.openDialog);\n  container.appendChild(pill);\n}\nconst state = String(block.props.find((p) => p.name === 'connectionState')?.value || 'disconnected');\nlet tone = 'off';\nlet text = 'Not connected';\nlet hint = 'Connect over USB';\nif (state === 'connecting') {\n  tone = 'wait';\n  text = 'Connecting\\u2026';\n  hint = 'Opening the serial port';\n} else if (state === 'connected:running') {\n  // What this board would send against what the device holds (see\n  // devkitCircuit.isDevkitDirty) -- read from the device on connect.\n  const dirty = helpers.devkit.dirty ? helpers.devkit.dirty() : false;\n  tone = dirty ? 'warn' : 'on';\n  text = dirty ? 'Running \\u00b7 circuit not saved' : 'Logic Module running';\n  hint = dirty ? 'Save the circuit to the device' : 'Connected';\n} else if (state.startsWith('connected:needs-firmware:')) {\n  tone = 'warn';\n  text = state.slice('connected:needs-firmware:'.length) + ' \\u00b7 needs firmware';\n  hint = 'Install the Logic Module firmware';\n} else if (state.startsWith('connected')) {\n  tone = 'warn';\n  text = 'Connected \\u00b7 no firmware answer';\n  hint = 'The board did not answer ping';\n}\npill.dataset.tone = tone;\npill.querySelector('.esp-status-text').textContent = text;\npill.style.setProperty('--esp-scale', String(helpers.contentScale || 1));\npill.title = hint + ' \\u2014 connection settings';"
          },
          {
            "id": "prp_mtg8v3eue",
            "name": "dialog",
            "kind": "value",
            "value": "container.style.fontFamily = 'Inter, sans-serif';\ncontainer.style.minWidth = '420px';\n\nconst heading = document.createElement('h3');\nheading.textContent = String(block.name || 'ESP32 DEVKIT').toUpperCase();\nheading.style.cssText = 'margin:0 0 14px;color:var(--success,#3ecf5d);font-size:15px;letter-spacing:.03em;';\ncontainer.appendChild(heading);\n\nif (!helpers.serial || !helpers.serial.isSupported()) {\n  const notSupported = document.createElement('p');\n  notSupported.style.cssText = 'margin:0;font-size:12px;color:var(--text-muted);';\n  notSupported.textContent = 'Web Serial is not available in this browser (use Chrome or Edge on desktop).';\n  container.appendChild(notSupported);\n} else {\n  const btnStyle = 'padding:6px 12px;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-primary);font-size:12px;cursor:pointer;';\n  const primaryBtnStyle = 'padding:6px 14px;border:1px solid var(--success,#3ecf5d);border-radius:6px;background:var(--success,#3ecf5d);color:#06210f;font-size:12px;font-weight:600;cursor:pointer;';\n  const sectionStyle = 'margin:0 0 16px;padding-bottom:16px;border-bottom:1px solid var(--border);';\n  const labelStyle = 'font-size:11px;font-weight:700;letter-spacing:.05em;color:var(--text-muted);margin-bottom:8px;';\n\n  function childSnapshot() {\n    if (!block.children) return '';\n    // Connections too, not just the blocks themselves -- rewiring which pin\n    // feeds which (with no other prop change at all) used to leave this\n    // dialog reporting \"Circuit matches what is on the device\" even though\n    // buildMinimalDesign's own belt routing (see serialConsole.js) would\n    // produce a genuinely different design.json for the new wiring.\n    const blocksSnap = Array.from(block.children.blocks.values()).map((c) => ({ id: c.id, props: c.props }));\n    const connsSnap = Array.from(block.children.connections.values()).map((cn) => ({\n      s: cn.sourceBlockId, sp: cn.sourcePortId, t: cn.targetBlockId, tp: cn.targetPortId,\n    }));\n    return JSON.stringify({ blocksSnap, connsSnap });\n  }\n\n  const statusEl = document.createElement('p');\n  statusEl.style.cssText = 'margin:0 0 12px;font-size:12px;color:var(--text-muted);';\n  container.appendChild(statusEl);\n\n  const connectRow = document.createElement('div');\n  connectRow.style.cssText = 'display:flex;gap:8px;margin-bottom:16px;';\n  const connectBtn = document.createElement('button');\n  connectBtn.type = 'button';\n  connectBtn.textContent = 'Connect (COM)';\n  connectBtn.style.cssText = btnStyle;\n  const disconnectBtn = document.createElement('button');\n  disconnectBtn.type = 'button';\n  disconnectBtn.textContent = 'Disconnect';\n  disconnectBtn.style.cssText = btnStyle;\n  disconnectBtn.disabled = true;\n  const wifiBtn = document.createElement('button');\n  wifiBtn.type = 'button';\n  wifiBtn.textContent = 'Connect (WiFi)';\n  wifiBtn.style.cssText = btnStyle;\n  connectRow.append(connectBtn, wifiBtn, disconnectBtn);\n  container.appendChild(connectRow);\n\n  // Shown only once a port is open but no firmware answered 'ping' --\n  // either a genuinely blank chip, or one running something else entirely.\n  const firmwareSection = document.createElement('div');\n  firmwareSection.style.cssText = sectionStyle + 'display:none;';\n  const firmwareLabel = document.createElement('div');\n  firmwareLabel.textContent = 'NO FIRMWARE DETECTED';\n  firmwareLabel.style.cssText = labelStyle;\n  firmwareSection.appendChild(firmwareLabel);\n  const firmwareRow = document.createElement('div');\n  firmwareRow.style.cssText = 'display:flex;gap:8px;align-items:center;';\n  const firmwareSelect = document.createElement('select');\n  firmwareSelect.style.cssText = 'flex:1;padding:5px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;';\n  const presets = (helpers.serial.firmwarePresets || []).filter((p) => !p.board || p.board === props.boardVariant);\n  for (const preset of presets) {\n    const option = document.createElement('option');\n    option.value = preset.id;\n    option.textContent = preset.label;\n    firmwareSelect.appendChild(option);\n  }\n  const preferredPreset = presets.find((p) => p.id === props.firmwarePreset) || presets.find((p) => p.chip === props.chipFamily);\n  if (preferredPreset) firmwareSelect.value = preferredPreset.id;\n  const installBtn = document.createElement('button');\n  installBtn.type = 'button';\n  installBtn.textContent = 'Install firmware';\n  installBtn.style.cssText = primaryBtnStyle;\n  installBtn.disabled = !presets.length;\n  firmwareRow.append(firmwareSelect, installBtn);\n  firmwareSection.appendChild(firmwareRow);\n  if (!presets.length) {\n    const noPresets = document.createElement('p');\n    noPresets.textContent = 'No firmware published yet for this chip.';\n    noPresets.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n    firmwareSection.appendChild(noPresets);\n  }\n  const progressEl = document.createElement('div');\n  progressEl.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n  firmwareSection.appendChild(progressEl);\n  container.appendChild(firmwareSection);\n\n  // Shown only once the board's own console confirmed it's actually\n  // running -- this is where the \"fixed hardware modules\" live; for now\n  // that's just Digital I/O (see the module's own children), CAN/WiFi etc.\n  // are future, separate submodules, not this one's job.\n  const programSection = document.createElement('div');\n  programSection.style.cssText = sectionStyle + 'display:none;';\n  const programLabel = document.createElement('div');\n  programLabel.textContent = 'CIRCUIT';\n  programLabel.style.cssText = labelStyle;\n  programSection.appendChild(programLabel);\n  const programStatus = document.createElement('p');\n  programStatus.style.cssText = 'margin:0 0 8px;font-size:11px;color:var(--text-muted);';\n  programSection.appendChild(programStatus);\n  const sendBtn = document.createElement('button');\n  sendBtn.type = 'button';\n  sendBtn.textContent = 'Save changes to device';\n  sendBtn.style.cssText = btnStyle;\n  programSection.appendChild(sendBtn);\n\n  const loadBtn = document.createElement('button');\n  loadBtn.type = 'button';\n  loadBtn.textContent = 'Load circuit from device';\n  loadBtn.style.cssText = btnStyle + 'margin-left:8px;';\n  loadBtn.style.display = 'none';\n  loadBtn.addEventListener('click', async () => {\n    if (!helpers.console.loadFromDevice) return;\n    loadBtn.disabled = true;\n    try {\n      const result = await helpers.console.loadFromDevice();\n      log(result.message);\n      updateProgramSection();\n    } catch (err) {\n      log('Load failed: ' + err.message);\n    } finally {\n      loadBtn.disabled = false;\n    }\n  });\n  programSection.appendChild(loadBtn);\n\n  // Live hardware override -- separate from \"Save changes to device\" above,\n  // which only ever declares pins (see buildMinimalDesign's own doc on why\n  // it can't yet compile real wiring). This talks straight to ioWrite() on\n  // the board (see esp32_logic's `io <gpio> <0|1>` serial command), the same\n  // direct-pin override its own browser circuit editor already does over\n  // WebSocket -- bypasses circuit logic entirely, on both ends.\n  const pinsLabel = document.createElement('div');\n  pinsLabel.textContent = 'LIVE PINS';\n  pinsLabel.style.cssText = labelStyle + 'margin-top:14px;';\n  programSection.appendChild(pinsLabel);\n  const pinsListEl = document.createElement('div');\n  pinsListEl.style.cssText = 'display:flex;flex-direction:column;gap:6px;';\n  programSection.appendChild(pinsListEl);\n  container.appendChild(programSection);\n\n  // ── Connectivity: who is around, and the board's radios — read and\n  // driven through the board's own shell (nodes / wifi status / wifi list /\n  // wifi select + pw / wifi on|off / ap on|off), the same commands a\n  // terminal on the USB port, or a CAN hop, would type. ──\n  const netSection = document.createElement('div');\n  netSection.style.cssText = sectionStyle + 'display:none;';\n  const netLabel = document.createElement('div');\n  netLabel.textContent = 'CONNECTIVITY';\n  netLabel.style.cssText = labelStyle;\n  netSection.appendChild(netLabel);\n  const nodesEl = document.createElement('div');\n  nodesEl.style.cssText = 'font-family:ui-monospace,Consolas,monospace;font-size:11px;color:var(--text-secondary);white-space:pre-wrap;margin-bottom:10px;';\n  nodesEl.textContent = 'Nodes: ...';\n  netSection.appendChild(nodesEl);\n  function radioRow(labelText) {\n    const row = document.createElement('label');\n    row.style.cssText = 'display:flex;align-items:center;gap:8px;font-size:12px;margin-bottom:6px;cursor:pointer;';\n    const box = document.createElement('input');\n    box.type = 'checkbox';\n    const name = document.createElement('span');\n    name.textContent = labelText;\n    name.style.cssText = 'font-weight:600;min-width:90px;';\n    const status = document.createElement('span');\n    status.style.cssText = 'color:var(--text-muted);flex:1;';\n    row.append(box, name, status);\n    netSection.appendChild(row);\n    return { box, status };\n  }\n  const apRow = radioRow('Access point');\n  const staRow = radioRow('WiFi');\n  const staControls = document.createElement('div');\n  staControls.style.cssText = 'display:none;flex-direction:column;gap:6px;margin:4px 0 8px 24px;';\n  const scanRow = document.createElement('div');\n  scanRow.style.cssText = 'display:flex;gap:6px;';\n  const netSelect = document.createElement('select');\n  netSelect.style.cssText = 'flex:1;min-width:0;padding:5px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;';\n  const scanBtn = document.createElement('button');\n  scanBtn.type = 'button';\n  scanBtn.textContent = 'Scan';\n  scanBtn.style.cssText = btnStyle;\n  scanRow.append(netSelect, scanBtn);\n  const pwRow = document.createElement('div');\n  pwRow.style.cssText = 'display:flex;gap:6px;';\n  const pwInput = document.createElement('input');\n  pwInput.type = 'password';\n  pwInput.placeholder = 'Password';\n  pwInput.style.cssText = 'flex:1;min-width:0;padding:5px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;';\n  const joinBtn = document.createElement('button');\n  joinBtn.type = 'button';\n  joinBtn.textContent = 'Connect';\n  joinBtn.style.cssText = primaryBtnStyle;\n  const forgetBtn = document.createElement('button');\n  forgetBtn.type = 'button';\n  forgetBtn.textContent = 'Forget';\n  forgetBtn.style.cssText = btnStyle;\n  pwRow.append(pwInput, joinBtn, forgetBtn);\n  staControls.append(scanRow, pwRow);\n  netSection.appendChild(staControls);\n  const netHint = document.createElement('div');\n  netHint.style.cssText = 'font-size:11px;color:var(--text-muted);';\n  netSection.appendChild(netHint);\n  container.appendChild(netSection);\n\n  // ── Shell: the board's command line itself. Type help. ──\n  const shellSection = document.createElement('div');\n  shellSection.style.cssText = sectionStyle.replace('border-bottom:1px solid var(--border);', '');\n  const shellLabel = document.createElement('div');\n  shellLabel.textContent = 'SHELL';\n  shellLabel.style.cssText = labelStyle + 'margin-bottom:0;';\n  const shellHeader = document.createElement('div');\n  shellHeader.style.cssText = 'display:flex;align-items:center;justify-content:space-between;margin-bottom:8px;';\n  const copyBtn = document.createElement('button');\n  copyBtn.type = 'button';\n  copyBtn.textContent = 'Copy';\n  copyBtn.title = 'Copy the whole shell output to the clipboard';\n  copyBtn.style.cssText = btnStyle + 'padding:3px 10px;font-size:11px;';\n  shellHeader.append(shellLabel, copyBtn);\n  const shellOut = document.createElement('pre');\n  shellOut.style.cssText = 'margin:0 0 6px;padding:8px;border:1px solid var(--border);border-radius:6px;background:var(--bg-well);color:var(--text-secondary);font-family:ui-monospace,Consolas,monospace;font-size:11px;white-space:pre-wrap;word-break:break-word;max-height:160px;overflow:auto;';\n  shellOut.textContent = String(props.connectionState || 'disconnected') + '\\ntype help';\n  const shellInput = document.createElement('input');\n  shellInput.type = 'text';\n  shellInput.placeholder = '> command';\n  shellInput.style.cssText = 'width:100%;box-sizing:border-box;padding:6px 8px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-family:ui-monospace,Consolas,monospace;font-size:12px;';\n  shellSection.append(shellHeader, shellOut, shellInput);\n  const shellHistory = shellOut.textContent.split('\\n');\n  copyBtn.addEventListener('click', async () => {\n    try {\n      await navigator.clipboard.writeText(shellHistory.join('\\n'));\n      copyBtn.textContent = 'Copied';\n    } catch (err) {\n      copyBtn.textContent = 'Copy failed';\n    }\n    setTimeout(() => { copyBtn.textContent = 'Copy'; }, 1500);\n  });\n  function shellPrint(text) {\n    shellHistory.push(...String(text).split('\\n'));\n    while (shellHistory.length > 200) shellHistory.shift();\n    shellOut.textContent = shellHistory.join('\\n');\n    shellOut.scrollTop = shellOut.scrollHeight;\n  }\n  // What the board says on its own shows up here too: a CAN reply that\n  // lands after `can <text>` was already answered with ok, a circuit's\n  // [USB] line, a node heard. Pin chatter and the pin-state JSON stay\n  // out; a command's own reply is printed by the command below.\n  if (helpers.console.subscribeLines) {\n    const stopLines = helpers.console.subscribeLines(({ line, unsolicited }) => {\n      if (!container.isConnected) { stopLines(); return; }\n      if (!unsolicited || line === 'ok' || line.startsWith('{') || line.startsWith('[TCA9554]')) return;\n      shellPrint(line);\n    });\n  }\n  async function runShell(line) {\n    if (!helpers.console.shell) return { ok: false, lines: [], error: 'no shell in this noditron' };\n    return helpers.console.shell(line);\n  }\n  shellInput.addEventListener('keydown', async (event) => {\n    if (event.key !== 'Enter') return;\n    const line = shellInput.value.trim();\n    if (!line) return;\n    shellInput.value = '';\n    shellPrint('> ' + line);\n    try {\n      const r = await runShell(line);\n      if (r.lines.length) shellPrint(r.lines.join('\\n'));\n      if (!r.ok) shellPrint('error: ' + r.error);\n    } catch (err) {\n      shellPrint('error: ' + err.message);\n    }\n  });\n\n  let netTimer = null;\n  let netBusy = false;\n  // The WiFi switch was turned on but the board has no network saved yet:\n  // keep it on and show the controls until a network is joined.\n  let staSetup = false;\n  async function refreshConnectivity() {\n    if (netBusy || !container.isConnected) return;\n    netBusy = true;\n    try {\n      // Polls are short: a board without the shell's ok terminator (build\n      // 20260924g) answers at once and then leaves the reader waiting.\n      const asJson = (lines, pick) => lines.map((l) => { try { return JSON.parse(l); } catch (_) { return null; } }).find(pick);\n      const nodes = await runShell('nodes', { timeoutMs: 1500 });\n      let nodeLines = nodes.lines.filter((l) => /^(logic|cnc|node) [0-9a-f]{8}/.test(l));\n      const nodesJson = asJson(nodes.lines, (j) => j && Array.isArray(j.nodes));\n      if (!nodeLines.length && nodesJson) {\n        nodeLines = nodesJson.nodes.map((n) => n.type + ' ' + n.id + ' v' + n.version\n          + (n.self ? ' self' : ' seen ' + Math.round((n.ageMs || 0) / 1000) + 's ago via ' + (n.via || 'can')));\n      }\n      nodesEl.textContent = nodeLines.length ? 'Nodes:\\n' + nodeLines.join('\\n') : 'Nodes: ' + (nodes.lines.length ? 'unreadable reply' : nodes.error || 'no reply');\n      const status = await runShell('wifi status', { timeoutMs: 1500 });\n      let sta = status.lines.find((l) => /^sta (on|off)/.test(l)) || '';\n      let ap = status.lines.find((l) => /^ap (on|off)/.test(l)) || '';\n      const statusJson = asJson(status.lines, (j) => j && j.ap && j.sta);\n      if (!sta && !ap && statusJson) {\n        // Build 20260924g: JSON, and no on/off switches yet -- both radios are on.\n        ap = 'ap on: ' + statusJson.ap.ssid + ' ' + statusJson.ap.ip + ' clients=' + statusJson.ap.clients;\n        sta = statusJson.sta.connected\n          ? 'sta on: connected ' + statusJson.sta.ssid + ' ip=' + statusJson.sta.ip + ' rssi=' + statusJson.sta.rssi\n          : statusJson.sta.saved ? 'sta on: disconnected, saved ' + statusJson.sta.ssid : 'sta on: disconnected, no network saved';\n      }\n      if (sta.startsWith('sta on')) staSetup = false;\n      staRow.box.checked = staSetup || sta.startsWith('sta on');\n      staRow.status.textContent = sta.replace(/^sta (on|off):?\\s*/, '') || (sta ? (staRow.box.checked ? 'on' : 'off') : '');\n      apRow.box.checked = ap.startsWith('ap on');\n      apRow.status.textContent = ap.replace(/^ap (on|off):?\\s*/, '') || (ap ? (apRow.box.checked ? 'on' : 'off') : '');\n      staControls.style.display = staRow.box.checked ? 'flex' : 'none';\n      const all = nodes.lines.concat(status.lines);\n      // An old build answers a shell word with grbl's numeric error or with JSON; a\n      // board that is busy (restarting its radio, say) answers nothing for a while.\n      if (!nodes.ok && !status.ok) {\n        if (all.some((l) => /^error:\\d+$/.test(l) || l.startsWith('{'))) netHint.textContent = 'This firmware has no shell yet -- switches and scan need the update above.';\n        else netHint.textContent = 'No answer right now -- the board may be restarting its radio; trying again.';\n      } else if (/^No answer right now/.test(netHint.textContent)) {\n        netHint.textContent = '';\n      }\n    } catch (err) {\n      netHint.textContent = err.message;\n    } finally {\n      netBusy = false;\n    }\n  }\n  function startConnectivity() {\n    netSection.style.display = '';\n    refreshConnectivity();\n    if (netTimer) return;\n    netTimer = setInterval(() => {\n      if (!container.isConnected || netSection.style.display === 'none') {\n        clearInterval(netTimer);\n        netTimer = null;\n        return;\n      }\n      refreshConnectivity();\n    }, 4000);\n  }\n  apRow.box.addEventListener('change', async () => {\n    const r = await runShell(apRow.box.checked ? 'ap on' : 'ap off');\n    if (!r.ok) netHint.textContent = r.error;\n    refreshConnectivity();\n  });\n  staRow.box.addEventListener('change', async () => {\n    if (!staRow.box.checked) {\n      staSetup = false;\n      const r = await runShell('wifi off');\n      if (!r.ok) netHint.textContent = r.error;\n      refreshConnectivity();\n      return;\n    }\n    const r = await runShell('wifi on');\n    if (!r.ok && /no network saved/i.test(r.error || '')) {\n      staSetup = true;\n      staControls.style.display = 'flex';\n      netHint.textContent = 'No network saved yet: scan, pick one and enter its password.';\n      return;\n    }\n    if (!r.ok) netHint.textContent = r.error;\n    refreshConnectivity();\n  });\n  scanBtn.addEventListener('click', async () => {\n    scanBtn.disabled = true;\n    netHint.textContent = 'Scanning...';\n    try {\n      let r = await runShell('wifi list');\n      for (let i = 0; i < 15 && r.lines.some((l) => l.startsWith('scanning')); i += 1) {\n        await new Promise((resolve) => setTimeout(resolve, 1000));\n        r = await runShell('wifi list');\n      }\n      const nets = r.lines.map((l) => l.match(/^(\\d+) (.+?) (-?\\d+)dBm (secured|open)/)).filter(Boolean);\n      netSelect.replaceChildren();\n      for (const m of nets) {\n        const option = document.createElement('option');\n        option.value = m[2];\n        option.textContent = m[2] + ' (' + m[3] + ' dBm, ' + m[4] + ')';\n        netSelect.appendChild(option);\n      }\n      netHint.textContent = nets.length ? nets.length + ' network(s) in range.' : (r.error || 'No networks found.');\n    } catch (err) {\n      netHint.textContent = err.message;\n    } finally {\n      scanBtn.disabled = false;\n    }\n  });\n  joinBtn.addEventListener('click', async () => {\n    const ssid = netSelect.value;\n    if (!ssid) { netHint.textContent = 'Scan and pick a network first.'; return; }\n    joinBtn.disabled = true;\n    try {\n      const sel = await runShell('wifi select ' + ssid);\n      if (!sel.ok) { netHint.textContent = sel.error; return; }\n      const pw = await runShell('wifi pw ' + pwInput.value);\n      netHint.textContent = pw.ok ? 'Joining ' + ssid + '...' : pw.error;\n      if (pw.ok) staSetup = false;\n      pwInput.value = '';\n      setTimeout(refreshConnectivity, 3000);\n    } finally {\n      joinBtn.disabled = false;\n    }\n  });\n  forgetBtn.addEventListener('click', async () => {\n    await runShell('wifi forget');\n    refreshConnectivity();\n  });\n\n  function updateProgramSection() {\n    let compiledCount = 0;\n    try {\n      const compiled = helpers.console.buildDevkitDesign ? helpers.console.buildDevkitDesign() : { blocks: [] };\n      compiledCount = compiled.blocks.length;\n    } catch (_) {\n      compiledCount = 0;\n    }\n    // Compared as compiled designs (see devkitCircuit.isDevkitDirty): what\n    // Save would send against what the device was last confirmed to hold,\n    // read from the device itself once it is running (reconcile below).\n    const dirty = helpers.console.isDirty ? helpers.console.isDirty() : compiledCount > 0;\n    const onDevice = String((block.props.find((p) => p.name === 'deviceDesign') || {}).value || '');\n    programStatus.textContent = !compiledCount\n      ? (onDevice ? 'The device holds a circuit; nothing is built here yet.' : 'No supported circuit is connected to this ' + (block.name || 'ESP32') + ' yet.')\n      : dirty ? 'Circuit differs from the device -- save it to the device, or load the device\\'s.' : 'Circuit matches what is on the device.';\n    sendBtn.disabled = !compiledCount;\n    loadBtn.style.display = onDevice ? '' : 'none';\n  }\n  updateProgramSection();\n\n  // One row per Digital I/O child with a real pin set -- the exact same\n  // filter buildMinimalDesign uses, so \"what Save would send\" and \"what\n  // Live Pins shows\" never disagree about which children count.\n  function pinChildren() {\n    const children = block.children ? Array.from(block.children.blocks.values()) : [];\n    return children\n      .filter((c) => (c.props || []).find((p) => p.name === 'noditronKind')?.value === 'digital-io')\n      .map((c) => {\n        const pin = (c.props || []).find((p) => p.name === 'pin')?.value;\n        const direction = (c.props || []).find((p) => p.name === 'direction')?.value === 'output' ? 'output' : 'input';\n        const valueProp = (c.props || []).find((p) => p.name === 'value');\n        return { child: c, pin, direction, valueProp };\n      })\n      .filter((entry) => entry.pin !== null && entry.pin !== undefined && entry.pin !== '');\n  }\n\n  const pinRows = new Map(); // childId -> { row, dot, label, btn }\n\n  async function setChildValue(entry, on) {\n    if (entry.valueProp) entry.valueProp.value = on ? 1 : 0;\n    const bd = await import('/nodigraph/src/model/BlockDescription.js');\n    entry.child.description = bd.serializeBlockDescription(entry.child);\n    helpers.refresh();\n  }\n\n  function renderPinsList() {\n    const entries = pinChildren();\n    const seen = new Set();\n    for (const entry of entries) {\n      seen.add(entry.child.id);\n      let ui = pinRows.get(entry.child.id);\n      if (!ui) {\n        const row = document.createElement('div');\n        row.style.cssText = 'display:flex;align-items:center;gap:8px;font-size:12px;';\n        const dot = document.createElement('span');\n        dot.style.cssText = 'width:10px;height:10px;border-radius:50%;flex:none;border:2px solid var(--border);box-sizing:border-box;';\n        const label = document.createElement('span');\n        label.style.cssText = 'flex:1;color:var(--text-secondary);';\n        const btn = document.createElement('button');\n        btn.type = 'button';\n        btn.style.cssText = btnStyle + 'padding:3px 10px;font-size:11px;';\n        row.append(dot, label, btn);\n        pinsListEl.appendChild(row);\n        ui = { row, dot, label, btn };\n        pinRows.set(entry.child.id, ui);\n      }\n      const on = Number(entry.valueProp && entry.valueProp.value) >= 1;\n      ui.label.textContent = 'GPIO' + entry.pin + ' · ' + entry.direction.toUpperCase() + ' · ' + (entry.child.name || 'Digital I/O');\n      ui.dot.style.background = on ? '#3ecf5d' : 'transparent';\n      ui.dot.style.borderColor = on ? '#3ecf5d' : 'var(--border)';\n      if (entry.direction === 'output') {\n        ui.btn.style.display = '';\n        ui.btn.textContent = on ? 'Set LOW' : 'Set HIGH';\n        ui.btn.onclick = async () => {\n          const next = !on;\n          ui.btn.disabled = true;\n          try {\n            await helpers.console.setPin(entry.pin, next);\n            await setChildValue(entry, next);\n            renderPinsList();\n          } catch (err) {\n            log('Set GPIO' + entry.pin + ' failed: ' + err.message);\n          } finally {\n            ui.btn.disabled = false;\n          }\n        };\n      } else {\n        ui.btn.style.display = 'none';\n        ui.btn.onclick = null;\n      }\n    }\n    for (const [childId, ui] of pinRows) {\n      if (!seen.has(childId)) {\n        ui.row.remove();\n        pinRows.delete(childId);\n      }\n    }\n    pinsLabel.style.display = entries.length ? '' : 'none';\n  }\n  renderPinsList();\n\n  // Reflects hardware-side changes back into the UI (an input pin toggled\n  // by whatever it's wired to on the board, or another client's own live\n  // control) -- not just this dialog's own writes. Stops itself once the\n  // dialog closes (container.isConnected goes false) or the device stops\n  // answering; no explicit \"dialog closed\" hook exists to unsubscribe from\n  // otherwise (see dialogSystem.js's close(), which just clears innerHTML).\n  let pinsPollTimer = null;\n  function startPinsPoll() {\n    if (pinsPollTimer) return;\n    pinsPollTimer = setInterval(async () => {\n      if (!container.isConnected || programSection.style.display === 'none') {\n        clearInterval(pinsPollTimer);\n        pinsPollTimer = null;\n        return;\n      }\n      try {\n        const livePins = await helpers.console.readPins({ timeoutMs: 1500 });\n        const byGpio = new Map(livePins.map((p) => [p.gpio, p]));\n        for (const entry of pinChildren()) {\n          const live = byGpio.get(Number(entry.pin));\n          if (live && entry.valueProp) entry.valueProp.value = live.state ? 1 : 0;\n        }\n        renderPinsList();\n      } catch (_) {\n        // A missed poll just leaves the UI showing the last known state --\n        // no reason to log every dropped byte on a link that otherwise works.\n      }\n    }, 800);\n  }\n\n  // A truly blank chip also needs its own bootloader/partition table, not\n  // just the app image \"Install firmware\" above assumes is already there\n  // (the ordinary case -- factory-fresh or previously-flashed boards both\n  // already have one). Collapsed since most people never need it.\n  const advanced = document.createElement('details');\n  advanced.style.cssText = 'margin:0 0 12px;';\n  const summary = document.createElement('summary');\n  summary.textContent = 'Advanced: flash files manually';\n  summary.style.cssText = 'cursor:pointer;font-size:11px;color:var(--text-muted);margin-bottom:8px;';\n  advanced.appendChild(summary);\n  const rows = document.createElement('div');\n  advanced.appendChild(rows);\n  const addRowBtn = document.createElement('button');\n  addRowBtn.type = 'button';\n  addRowBtn.textContent = '+ Add file';\n  addRowBtn.style.cssText = btnStyle;\n  advanced.appendChild(addRowBtn);\n  const eraseRow = document.createElement('label');\n  eraseRow.style.cssText = 'display:flex;align-items:center;gap:8px;margin:12px 0;cursor:pointer;font-size:12px;';\n  const eraseCheckbox = document.createElement('input');\n  eraseCheckbox.type = 'checkbox';\n  eraseCheckbox.checked = true;\n  eraseRow.append(eraseCheckbox, document.createTextNode('Erase flash before writing (recommended for a blank board)'));\n  advanced.appendChild(eraseRow);\n  const flashBtn = document.createElement('button');\n  flashBtn.type = 'button';\n  flashBtn.textContent = 'Flash';\n  flashBtn.style.cssText = btnStyle;\n  advanced.appendChild(flashBtn);\n  container.appendChild(advanced);\n\n  // The connection log and the shell are one window: what the dialog\n  // reports and what the board answers, in order, at the bottom.\n  container.appendChild(shellSection);\n  function log(line) {\n    shellPrint(line);\n  }\n\n  let bootloaderOffset = null;\n\n  function showRunning(info) {\n    const updateAvailable = Boolean(preferredPreset && preferredPreset.build && info && info.build && info.build < preferredPreset.build);\n    firmwareSection.style.display = updateAvailable ? '' : 'none';\n    if (updateAvailable) {\n      firmwareLabel.textContent = 'FIRMWARE UPDATE AVAILABLE';\n      installBtn.textContent = 'Update firmware';\n      progressEl.textContent = 'Installed build ' + info.build + '; available build ' + preferredPreset.build + '.';\n    }\n    programSection.style.display = '';\n    updateProgramSection();\n    renderPinsList();\n    startPinsPoll();\n    startConnectivity();\n    if (helpers.console.reconcile) {\n      helpers.console.reconcile()\n        .then((result) => { log(result.message); updateProgramSection(); })\n        .catch((err) => log('Could not read the circuit on the device: ' + err.message));\n    }\n  }\n\n  function showNeedsFirmware() {\n    programSection.style.display = 'none';\n    firmwareSection.style.display = '';\n  }\n\n  // The one place that decides what's actually on the other end of the\n  // port: try the console's own 'ping' first (harmless if it's already\n  // running -- just a status line back), and only reset into the ROM\n  // bootloader if nothing answers. Reused after Connect, and again after\n  // Install firmware once the board's had a moment to boot into it.\n  async function afterPortOpen() {\n    statusEl.textContent = 'Connected -- checking for firmware...';\n    try {\n      const info = await helpers.console.identify();\n      if (info.verified) {\n        statusEl.textContent = 'Logic Module v' + info.version + ' build ' + info.build + ' running.';\n        log('ping: v' + info.version + ' build ' + info.build + ', heap=' + info.heap + '.');\n        helpers.setProp('connectionState', 'connected:running');\n        showRunning(info);\n        return;\n      }\n    } catch (err) {\n      log('Console check failed: ' + err.message);\n    }\n    try {\n      const detected = await helpers.serial.detectChip(log);\n      bootloaderOffset = detected.bootloaderOffset;\n      const family = detected.chipName.split(' ')[0];\n      const matchingPreset = presets.find((p) => p.chip === family && p.id === props.firmwarePreset) || presets.find((p) => p.chip === family);\n      firmwareSelect.value = matchingPreset ? matchingPreset.id : '';\n      for (const option of firmwareSelect.options) {\n        option.disabled = !presets.some((p) => p.id === option.value && p.chip === family);\n      }\n      installBtn.disabled = !matchingPreset;\n      log('Detected ' + detected.chipName + ' (bootloader @ 0x' + detected.bootloaderOffset.toString(16) + ').');\n      // Detecting the chip is not free: it drives the board into its ROM\n      // bootloader and uploads esptool's stub, and nothing there ever hands\n      // control back to the app. So by this point the board is not running\n      // whatever it has, and asking again while it sits like that would\n      // answer a question about our own doing rather than about the board.\n      // identify() resets it back out (see serialFlash's reopenPlain) and\n      // asks once more -- a board that was running all along says so here,\n      // instead of being reported as blank because we stopped it.\n      try {\n        const after = await helpers.console.identify();\n        if (after.verified) {\n          statusEl.textContent = 'Logic Module v' + after.version + ' build ' + after.build + ' running.';\n          log('ping: v' + after.version + ' build ' + after.build + ', heap=' + after.heap + '.');\n          helpers.setProp('connectionState', 'connected:running');\n          showRunning(after);\n          return;\n        }\n      } catch (err) {\n        log('Console re-check failed: ' + err.message);\n      }\n      statusEl.textContent = detected.chipName + ' detected -- logic firmware did not respond.';\n      helpers.setProp('connectionState', 'connected:needs-firmware:' + detected.chipName);\n      showNeedsFirmware();\n    } catch (err) {\n      statusEl.textContent = 'Connected, but could not identify the board.';\n      log('Chip detect failed: ' + err.message);\n      helpers.setProp('connectionState', 'connected:unknown');\n    }\n  }\n\n  const existing = helpers.serial.getSession();\n  if (existing) {\n    disconnectBtn.disabled = false;\n    connectBtn.disabled = true;\n    wifiBtn.disabled = true;\n    afterPortOpen();\n  } else {\n    statusEl.textContent = 'Not connected.';\n  }\n\n  connectBtn.addEventListener('click', async () => {\n    connectBtn.disabled = true;\n    try {\n      await helpers.serial.connect(log);\n      log('Serial port opened.');\n      disconnectBtn.disabled = false;\n      wifiBtn.disabled = true;\n      await afterPortOpen();\n    } catch (err) {\n      statusEl.textContent = 'Connect failed: ' + err.message;\n      connectBtn.disabled = false;\n    }\n  });\n\n  // The same board over the air: the board's WebSocket console stands in\n  // for the serial port (see noditron's wifiTransport.js), designs go by\n  // HTTP, and a firmware update goes through the board's OTA endpoint.\n  wifiBtn.addEventListener('click', async () => {\n    if (!helpers.serial.connectWifi) { log('This noditron has no WiFi link yet.'); return; }\n    const last = (helpers.serial.rememberedWifi && helpers.serial.rememberedWifi()) || '192.168.0.1';\n    const host = window.prompt('Board address: its IP or name on your network, or 192.168.0.1 on its own access point.', last);\n    if (!host) return;\n    wifiBtn.disabled = true;\n    connectBtn.disabled = true;\n    try {\n      await helpers.serial.connectWifi(host, log);\n      disconnectBtn.disabled = false;\n      await afterPortOpen();\n    } catch (err) {\n      statusEl.textContent = 'WiFi connect failed: ' + err.message;\n      log('WiFi connect failed: ' + err.message);\n      wifiBtn.disabled = false;\n      connectBtn.disabled = false;\n    }\n  });\n\n  disconnectBtn.addEventListener('click', async () => {\n    helpers.console.close();\n    await helpers.serial.disconnect(true);\n    statusEl.textContent = 'Disconnected.';\n    connectBtn.disabled = false;\n    wifiBtn.disabled = false;\n    disconnectBtn.disabled = true;\n    firmwareSection.style.display = 'none';\n    programSection.style.display = 'none';\n    netSection.style.display = 'none';\n    helpers.setProp('connectionState', 'disconnected');\n  });\n\n  installBtn.addEventListener('click', async () => {\n    const preset = presets.find((p) => p.id === firmwareSelect.value);\n    if (!preset) return;\n    installBtn.disabled = true;\n    const original = installBtn.textContent;\n    installBtn.textContent = 'Installing...';\n    try {\n      const files = await helpers.serial.fetchPresetParts(preset);\n      log('Fetched ' + preset.label + ' (' + files.map((f) => f.name).join(', ') + ').');\n      // Every part of the preset, written together in one writeFlash call --\n      // never just the app image alone. A board that only ever gets its app\n      // partition rewritten keeps whatever bootloader/partition table it\n      // already has, which is fine right up until one doesn't have either\n      // (a genuinely blank or fully erased chip) -- then the ROM loops\n      // \"invalid header: 0xffffffff\" forever with nothing to hand off to.\n      // Rewriting an unchanged bootloader/partition table on a board that\n      // already had one is a harmless no-op, so always including them here\n      // costs nothing on the common case and fixes the blank-chip one.\n      await helpers.serial.flash(\n        files,\n        false,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + '/' + files.length + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      progressEl.textContent = '';\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Install failed: ' + err.message);\n    } finally {\n      installBtn.disabled = false;\n      installBtn.textContent = original;\n    }\n  });\n\n  sendBtn.addEventListener('click', async () => {\n    sendBtn.disabled = true;\n    const original = sendBtn.textContent;\n    sendBtn.textContent = 'Saving...';\n    try {\n      const children = block.children ? Array.from(block.children.blocks.values()) : [];\n      const connections = block.children ? Array.from(block.children.connections.values()) : [];\n      const design = helpers.console.buildMinimalDesign(children, connections);\n      if (!design.blocks.length) {\n        log('Nothing to send -- connect a supported circuit signal directly to DI, DO, CAN In, or CAN Out.');\n      } else {\n        await helpers.console.sendDesign(design);\n        if (helpers.console.markDevkitSent) helpers.console.markDevkitSent(design);\n        else helpers.setProp('lastSentSnapshot', childSnapshot());\n        const pinCount = design.blocks.filter((b) => b.type === 'din' || b.type === 'dout').length;\n        const beltCount = design.blocks.length - pinCount;\n        log('Saved ' + pinCount + ' pin(s)' + (beltCount ? ' and ' + beltCount + ' belt(s)' : '') + ' to the device. It applies immediately and survives reboot.');\n        updateProgramSection();\n      }\n    } catch (err) {\n      log('Save failed: ' + err.message);\n    } finally {\n      sendBtn.disabled = false;\n      sendBtn.textContent = original;\n    }\n  });\n\n  function addFileRow() {\n    const row = document.createElement('div');\n    row.style.cssText = 'display:flex;align-items:center;gap:8px;margin-bottom:6px;';\n    const fileInput = document.createElement('input');\n    fileInput.type = 'file';\n    fileInput.accept = '.bin';\n    fileInput.style.cssText = 'flex:1;font-size:11px;min-width:0;';\n    const addrInput = document.createElement('input');\n    addrInput.type = 'text';\n    addrInput.style.cssText = 'width:90px;flex:none;padding:4px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;font-family:ui-monospace,monospace;box-sizing:border-box;';\n    addrInput.value = '0x10000';\n    const removeBtn = document.createElement('button');\n    removeBtn.type = 'button';\n    removeBtn.textContent = String.fromCharCode(215);\n    removeBtn.style.cssText = 'width:22px;height:22px;flex:none;padding:0;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-muted);cursor:pointer;';\n    removeBtn.addEventListener('click', () => row.remove());\n    fileInput.addEventListener('change', () => {\n      const f = fileInput.files[0];\n      if (f) addrInput.value = '0x' + helpers.serial.guessAddress(f.name, bootloaderOffset).toString(16);\n    });\n    row.append(fileInput, addrInput, removeBtn);\n    rows.appendChild(row);\n  }\n  addFileRow();\n  addRowBtn.addEventListener('click', () => addFileRow());\n\n  flashBtn.addEventListener('click', async () => {\n    const files = [];\n    for (const row of rows.children) {\n      const addrInput = row.querySelector('input[type=text]');\n      const address = parseInt(addrInput.value, 16) || 0;\n      const fileInput = row.querySelector('input[type=file]');\n      const file = fileInput && fileInput.files[0];\n      if (!file) continue;\n      files.push({ name: file.name, file, address });\n    }\n    if (!files.length) { log('Pick at least one .bin file first.'); return; }\n    flashBtn.disabled = true;\n    try {\n      await helpers.serial.flash(\n        files,\n        eraseCheckbox.checked,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Flash failed: ' + err.message);\n    } finally {\n      flashBtn.disabled = false;\n    }\n  });\n}"
          },
          {
            "id": "prp_mtg8v3ep8",
            "name": "allowedChildKinds",
            "kind": "value",
            "value": "[\"digital-io\",\"timer\",\"and\",\"or\",\"not\",\"data\",\"add\",\"boot\",\"serial\",\"serialin\",\"can\",\"i2cout\",\"i2cin\",\"pwmout\",\"pwmin\",\"slice\",\"route\",\"croute\"]"
          },
          {
            "id": "prp_esp32s3_pinmap",
            "name": "pinMap",
            "kind": "value",
            "value": "[{\"label\":\"DI1\",\"gpio\":4,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"DI2\",\"gpio\":5,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"DI3\",\"gpio\":6,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"DI4\",\"gpio\":7,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"DI5\",\"gpio\":8,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"DI6\",\"gpio\":9,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"DI7\",\"gpio\":10,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"DI8\",\"gpio\":11,\"role\":\"digital-input\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"CAN In\",\"gpio\":3,\"role\":\"can-in\",\"inputOnly\":true,\"outputOnly\":false,\"side\":\"left\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"DO1\",\"gpio\":1000,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":1,\"note\":\"TCA9554 EXIO1 / isolated Darlington output\",\"side\":\"right\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"DO2\",\"gpio\":1001,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":2,\"note\":\"TCA9554 EXIO2 / isolated Darlington output\",\"side\":\"right\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"DO3\",\"gpio\":1002,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":3,\"note\":\"TCA9554 EXIO3 / isolated Darlington output\",\"side\":\"right\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"DO4\",\"gpio\":1003,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":4,\"note\":\"TCA9554 EXIO4 / isolated Darlington output\",\"side\":\"right\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"DO5\",\"gpio\":1004,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":5,\"note\":\"TCA9554 EXIO5 / isolated Darlington output\",\"side\":\"right\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"DO6\",\"gpio\":1005,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":6,\"note\":\"TCA9554 EXIO6 / isolated Darlington output\",\"side\":\"right\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"DO7\",\"gpio\":1006,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":7,\"note\":\"TCA9554 EXIO7 / isolated Darlington output\",\"side\":\"right\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"DO8\",\"gpio\":1007,\"role\":\"digital-output\",\"inputOnly\":false,\"outputOnly\":true,\"exio\":8,\"note\":\"TCA9554 EXIO8 / isolated Darlington output\",\"side\":\"right\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"CAN Out\",\"gpio\":2,\"role\":\"can-out\",\"inputOnly\":false,\"outputOnly\":true,\"side\":\"right\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"CAN speed\",\"gpio\":null,\"role\":\"can-speed\",\"inputOnly\":false,\"outputOnly\":true,\"side\":\"right\",\"row\":9,\"orientation\":\"usb-top\"},{\"label\":\"RS485 TX\",\"gpio\":17,\"role\":\"rs485\",\"inputOnly\":false,\"outputOnly\":false,\"side\":\"right\",\"row\":10,\"orientation\":\"usb-top\",\"reserved\":\"RS485 TX\"},{\"label\":\"RS485 RX\",\"gpio\":18,\"role\":\"rs485\",\"inputOnly\":false,\"outputOnly\":false,\"side\":\"right\",\"row\":11,\"orientation\":\"usb-top\",\"reserved\":\"RS485 RX\"},{\"label\":\"USB\",\"gpio\":null,\"role\":\"usb-serial\",\"inputOnly\":null,\"side\":\"top\",\"row\":null,\"orientation\":\"usb-top\"}]"
          },
          {
            "id": "prp_esp32s3_render",
            "name": "render",
            "kind": "value",
            "value": "const g = block.geometry;\nconst pinMapProp = block.props.find((p) => p.name === 'pinMap');\nlet pins = [];\ntry { pins = JSON.parse(pinMapProp ? pinMapProp.value : '[]'); } catch (_) { pins = []; }\n\nctx.save();\nctx.fillStyle = '#ffffff';\nctx.fillRect(g.x + 12, g.y + 8, g.width - 24, g.height - 16);\n\nctx.fillStyle = '#1f2937';\nctx.font = '20px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textAlign = 'center';\nctx.textBaseline = 'middle';\nctx.fillText(block.name || 'esp32-S3', g.x + g.width / 2, g.y + g.height / 2 + 30, g.width - 48);\n\n\nconst topY = g.y + 58;\nfunction drawBoardButton(cx, label, fill) {\n  ctx.beginPath();\n  ctx.arc(cx, topY, 13, 0, Math.PI * 2);\n  ctx.fillStyle = fill;\n  ctx.fill();\n  ctx.lineWidth = 1;\n  ctx.strokeStyle = '#94a3b8';\n  ctx.stroke();\n  ctx.fillStyle = '#6b7280';\n  ctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\n  ctx.textAlign = 'center';\n  ctx.fillText(label, cx, topY + 25);\n}\n// The BOOT button sits under the GPIO0 pin, wherever that pin is on the\n// top edge (nodigraph draws a pin on the nearest 40-unit slot: 20, 60, ...).\nconst bootLogical = (block.logicalPorts || []).find((lp) => lp.name === 'GPIO0');\nconst bootPin = bootLogical && (block.ports || []).find((p) => p.logicalId === bootLogical.id && p.side === 'top');\nconst onSlot = (o) => Math.max(20, Math.min(g.width - 20, Math.round((o - 20) / 40) * 40 + 20));\ndrawBoardButton(bootPin ? g.x + onSlot(Number(bootPin.offset)) : g.x + g.width / 2 - 72, 'GPIO0', '#f8fafc');\ndrawBoardButton(g.x + g.width / 2 + 72, 'RESET', '#f8fafc');\n\nctx.beginPath();\nctx.arc(g.x + g.width / 2, topY, 7, 0, Math.PI * 2);\nctx.fillStyle = '#3ecf5d';\nctx.fill();\nctx.fillStyle = '#6b7280';\nctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.fillText('RGB GPIO38', g.x + g.width / 2, topY + 25);\n\nctx.font = '15px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textBaseline = 'middle';\nfor (const pin of pins) {\n  if (pin.side !== 'left' && pin.side !== 'right') continue;\n  const y = g.y + 30 + Number(pin.row || 0) * 40;\n  const left = pin.side === 'left';\n  const labelX = left ? g.x + 26 : g.x + g.width - 26;\n  ctx.fillStyle = pin.role === 'power' ? '#ef4444' : pin.role === 'ground' ? '#6b7280' : (pin.reserved || pin.inputOnly) ? '#94a3b8' : '#1f2937';\n  ctx.textAlign = left ? 'left' : 'right';\n  ctx.fillText(pin.label, labelX, y, 86);\n}\nctx.restore();"
          },
          {
            "id": "prp_esp32s3_boardvariant",
            "name": "boardVariant",
            "kind": "value",
            "value": "ESP32-S3-POE-ETH-8DI-8DO"
          },
          {
            "id": "prp_esp32s3_usborientation",
            "name": "usbOrientation",
            "kind": "value",
            "value": "top"
          },
          {
            "id": "prp_esp32s3_onboardcontrols",
            "name": "onboardControls",
            "kind": "value",
            "value": "{\"buttons\":[{\"label\":\"GPIO0\",\"gpio\":0,\"role\":\"input\"},{\"label\":\"RESET\",\"gpio\":null,\"role\":\"reset\"}],\"led\":{\"label\":\"RGB\",\"gpio\":38,\"role\":\"output\",\"type\":\"addressable\"}}"
          },
          {
            "id": "prp_esp32s3_firmwarepreset",
            "name": "firmwarePreset",
            "kind": "value",
            "value": "logic-esp32-s3-waveshare"
          },
          {
            "id": "prp_esp32s3_canpins",
            "name": "canPins",
            "kind": "value",
            "value": "{\"tx\":2,\"rx\":3}"
          }
        ],
        "hasChildren": true,
        "boundaryGeometry": {
          "x": 0,
          "y": 0,
          "width": 320,
          "height": 240
        },
        "requirementIds": []
      }
    ],
    "connections": []
  }
}
