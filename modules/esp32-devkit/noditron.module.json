{
  "noditronModule": 1,
  "name": "esp32-devkit",
  "displayName": "ESP32 DevKit",
  "version": "1.10.0",
  "description": "A classic ESP32 DevKit V1 board module with all board pins exposed on the block edge, USB at the top, and connection-gated logic-module setup.",
  "swatchColor": "#c98a2f",
  "block": {
    "format": "nodigraph/clipboard-v1",
    "blocks": [
      {
        "id": "blk_mtg8v3ep1",
        "name": "ESP32 DevKit",
        "type": "block",
        "kind": "block",
        "description": "Block: ESP32 DevKit\n\nport.3V3: power\nport.GND: ground\nport.D15: GPIO15\nport.D2: GPIO2\nport.D4: GPIO4\nport.RX2: GPIO16\nport.TX2: GPIO17\nport.D5: GPIO5\nport.D18: GPIO18\nport.D19: GPIO19\nport.D21: GPIO21\nport.RX0: GPIO3\nport.TX0: GPIO1\nport.D22: GPIO22\nport.D23: GPIO23\nport.VIN: power\nport.GND: ground\nport.D13: GPIO13\nport.D12: GPIO12\nport.D14: GPIO14\nport.D27: GPIO27\nport.D26: GPIO26\nport.D25: GPIO25\nport.D33: GPIO33\nport.D32: GPIO32\nport.D35: GPIO35 input only\nport.D34: GPIO34 input only\nport.VN: GPIO39 input only\nport.VP: GPIO36 input only\nport.EN: enable\nport.GPIO0: GPIO0 BOOT button (reads 0 while pressed)\nport.USB: USB serial: what the circuit sends here reaches the browser\n\nprop.noditronKind: esp32-devkit\nprop.chipFamily: ESP32\nprop.connectionState: disconnected\nprop.lastSentSnapshot: \nprop.render: const g = block.geometry;\nconst pinMapProp = block.props.find((p) => p.name === 'pinMap');\nlet pins = [];\ntry { pins = JSON.parse(pinMapProp ? pinMapProp.value : '[]'); } catch (_) { pins = []; }\n\nctx.save();\nctx.fillStyle = '#ffffff';\nctx.fillRect(g.x + 12, g.y + 8, g.width - 24, g.height - 16);\n\nctx.fillStyle = '#1f2937';\nctx.font = '20px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textAlign = 'center';\nctx.textBaseline = 'middle';\nctx.fillText(block.name || 'ESP32 DevKit', g.x + g.width / 2, g.y + g.height / 2 + 30, g.width - 48);\n\nctx.fillStyle = '#e5e7eb';\nctx.strokeStyle = '#9ca3af';\nctx.lineWidth = 1;\nctx.beginPath();\nctx.roundRect(g.x + g.width / 2 - 36, g.y + 14, 72, 30, 4);\nctx.fill();\nctx.stroke();\nctx.fillStyle = '#6b7280';\nctx.font = '10px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.fillText('USB', g.x + g.width / 2, g.y + 29);\n\nconst topY = g.y + 58;\nfunction drawBoardButton(cx, label, fill) {\n  ctx.beginPath();\n  ctx.arc(cx, topY, 13, 0, Math.PI * 2);\n  ctx.fillStyle = fill;\n  ctx.fill();\n  ctx.lineWidth = 1;\n  ctx.strokeStyle = '#94a3b8';\n  ctx.stroke();\n  ctx.fillStyle = '#6b7280';\n  ctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\n  ctx.textAlign = 'center';\n  ctx.fillText(label, cx, topY + 25);\n}\ndrawBoardButton(g.x + g.width / 2 - 72, 'GPIO0', '#f8fafc');\ndrawBoardButton(g.x + g.width / 2 + 72, 'GPIO35', '#f8fafc');\n\nctx.beginPath();\nctx.arc(g.x + g.width / 2, topY, 7, 0, Math.PI * 2);\nctx.fillStyle = '#3ecf5d';\nctx.fill();\nctx.fillStyle = '#6b7280';\nctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.fillText('LED GPIO2', g.x + g.width / 2, topY + 25);\n\nctx.font = '15px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textBaseline = 'middle';\nfor (const pin of pins) {\n  const y = g.y + 30 + Number(pin.row || 0) * 40;\n  const left = pin.side === 'left';\n  const labelX = left ? g.x + 26 : g.x + g.width - 26;\n  ctx.fillStyle = pin.role === 'power' ? '#ef4444' : pin.role === 'ground' ? '#6b7280' : pin.inputOnly ? '#64748b' : '#1f2937';\n  ctx.textAlign = left ? 'left' : 'right';\n  ctx.fillText(pin.label, labelX, y, 86);\n}\nctx.restore();\nprop.html: container.style.position = 'relative';\ncontainer.style.display = 'flex';\ncontainer.style.flexDirection = 'column';\ncontainer.style.boxSizing = 'border-box';\ncontainer.style.padding = '8px';\ncontainer.style.fontFamily = 'Inter, sans-serif';\n\nlet row = container.querySelector('.esp-status-row');\nif (!row) {\n  row = document.createElement('div');\n  row.className = 'esp-status-row';\n  row.style.cssText = 'display:flex;align-items:center;gap:6px;max-width:calc(100% - 26px);';\n  const dot = document.createElement('span');\n  dot.className = 'esp-status-dot';\n  dot.style.cssText = 'width:9px;height:9px;border-radius:50%;flex:none;background:var(--border);';\n  const label = document.createElement('span');\n  label.className = 'esp-status-label';\n  label.style.cssText = 'font-size:10px;font-weight:700;letter-spacing:.03em;color:var(--text-muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;';\n  row.append(dot, label);\n\n  const dirty = document.createElement('div');\n  dirty.className = 'esp-dirty-row';\n  dirty.style.cssText = 'font-size:9px;color:#e5b93e;margin-top:2px;';\n\n  const gear = document.createElement('button');\n  gear.type = 'button';\n  gear.className = 'esp-gear';\n  gear.textContent = String.fromCharCode(9881);\n  gear.title = 'Configure';\n  gear.style.cssText = 'position:absolute;top:4px;right:4px;width:20px;height:20px;padding:0;border-radius:5px;border:1px solid var(--border);background:none;color:var(--text-muted);font-size:11px;line-height:1;cursor:pointer;';\n  gear.addEventListener('click', helpers.openDialog);\n\n  container.append(row, dirty, gear);\n}\n\nconst state = String((block.props.find((p) => p.name === 'connectionState') || {}).value || 'disconnected');\nconst dot = container.querySelector('.esp-status-dot');\nconst label = container.querySelector('.esp-status-label');\nif (state === 'connected:running') {\n  dot.style.background = '#3ecf5d';\n  label.textContent = 'Logic Module running';\n} else if (state === 'disconnected' || !state) {\n  dot.style.background = 'var(--border)';\n  label.textContent = 'Not connected';\n} else if (state.indexOf('connected:needs-firmware:') === 0) {\n  dot.style.background = '#e5b93e';\n  label.textContent = state.slice('connected:needs-firmware:'.length) + ' -- needs firmware';\n} else {\n  dot.style.background = '#e5b93e';\n  label.textContent = 'Firmware check needed';\n}\n\nlet design = { blocks: [] };\nlet snapshot = '';\ntry {\n  design = helpers.devkit ? helpers.devkit.buildDesign() : { blocks: [] };\n  snapshot = helpers.devkit ? helpers.devkit.snapshot() : '';\n} catch (_) {\n  design = { blocks: [] };\n}\nconst summary = helpers.devkit ? helpers.devkit.summary(design) : { blockCount: design.blocks.length };\nconst lastSent = (block.props.find((p) => p.name === 'lastSentSnapshot') || {}).value || '';\nconst dirtyRow = container.querySelector('.esp-dirty-row');\nif (!summary.blockCount) dirtyRow.textContent = '';\nelse if (state !== 'connected:running') dirtyRow.textContent = 'Circuit ready -- connect firmware to send';\nelse dirtyRow.textContent = snapshot && snapshot === lastSent ? 'Circuit sent' : 'Circuit changed -- not sent';\nprop.dialog: container.style.fontFamily = 'Inter, sans-serif';\ncontainer.style.minWidth = '420px';\n\nconst heading = document.createElement('h3');\nheading.textContent = String(block.name || 'ESP32 DEVKIT').toUpperCase();\nheading.style.cssText = 'margin:0 0 14px;color:var(--success,#3ecf5d);font-size:15px;letter-spacing:.03em;';\ncontainer.appendChild(heading);\n\nif (!helpers.serial || !helpers.serial.isSupported()) {\n  const notSupported = document.createElement('p');\n  notSupported.style.cssText = 'margin:0;font-size:12px;color:var(--text-muted);';\n  notSupported.textContent = 'Web Serial is not available in this browser (use Chrome or Edge on desktop).';\n  container.appendChild(notSupported);\n} else {\n  const btnStyle = 'padding:6px 12px;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-primary);font-size:12px;cursor:pointer;';\n  const primaryBtnStyle = 'padding:6px 14px;border:1px solid var(--success,#3ecf5d);border-radius:6px;background:var(--success,#3ecf5d);color:#06210f;font-size:12px;font-weight:600;cursor:pointer;';\n  const sectionStyle = 'margin:0 0 16px;padding-bottom:16px;border-bottom:1px solid var(--border);';\n  const labelStyle = 'font-size:11px;font-weight:700;letter-spacing:.05em;color:var(--text-muted);margin-bottom:8px;';\n\n  function childSnapshot() {\n    if (!block.children) return '';\n    // Connections too, not just the blocks themselves -- rewiring which pin\n    // feeds which (with no other prop change at all) used to leave this\n    // dialog reporting \"Circuit matches what is on the device\" even though\n    // buildMinimalDesign's own belt routing (see serialConsole.js) would\n    // produce a genuinely different design.json for the new wiring.\n    const blocksSnap = Array.from(block.children.blocks.values()).map((c) => ({ id: c.id, props: c.props }));\n    const connsSnap = Array.from(block.children.connections.values()).map((cn) => ({\n      s: cn.sourceBlockId, sp: cn.sourcePortId, t: cn.targetBlockId, tp: cn.targetPortId,\n    }));\n    return JSON.stringify({ blocksSnap, connsSnap });\n  }\n\n  const statusEl = document.createElement('p');\n  statusEl.style.cssText = 'margin:0 0 12px;font-size:12px;color:var(--text-muted);';\n  container.appendChild(statusEl);\n\n  const connectionSection = document.createElement('div');\n  connectionSection.style.cssText = sectionStyle;\n  const connectionLabel = document.createElement('div');\n  connectionLabel.textContent = 'CONNECTION';\n  connectionLabel.style.cssText = labelStyle;\n  connectionSection.appendChild(connectionLabel);\n\n  const methodRow = document.createElement('div');\n  methodRow.style.cssText = 'display:flex;gap:6px;margin-bottom:10px;';\n  const usbMethodBtn = document.createElement('button');\n  usbMethodBtn.type = 'button';\n  usbMethodBtn.textContent = 'USB';\n  usbMethodBtn.style.cssText = primaryBtnStyle;\n  const wifiMethodBtn = document.createElement('button');\n  wifiMethodBtn.type = 'button';\n  wifiMethodBtn.textContent = 'WiFi';\n  wifiMethodBtn.style.cssText = btnStyle;\n  wifiMethodBtn.title = 'WiFi connection will be added later';\n  methodRow.append(usbMethodBtn, wifiMethodBtn);\n  connectionSection.appendChild(methodRow);\n\n  const usbPane = document.createElement('div');\n  const connectRow = document.createElement('div');\n  connectRow.style.cssText = 'display:flex;gap:8px;margin-bottom:8px;';\n  const connectBtn = document.createElement('button');\n  connectBtn.type = 'button';\n  connectBtn.textContent = 'Select serial port';\n  connectBtn.style.cssText = btnStyle;\n  const disconnectBtn = document.createElement('button');\n  disconnectBtn.type = 'button';\n  disconnectBtn.textContent = 'Disconnect';\n  disconnectBtn.style.cssText = btnStyle;\n  disconnectBtn.disabled = true;\n  connectRow.append(connectBtn, disconnectBtn);\n  usbPane.appendChild(connectRow);\n  const usbHint = document.createElement('p');\n  usbHint.style.cssText = 'margin:0;font-size:11px;color:var(--text-muted);';\n  usbHint.textContent = 'Chrome will show the available USB serial ports. Choose the ESP32 DevKit, then Noditron checks whether the logic firmware is already running.';\n  usbPane.appendChild(usbHint);\n  connectionSection.appendChild(usbPane);\n\n  const wifiPane = document.createElement('p');\n  wifiPane.style.cssText = 'display:none;margin:0;font-size:11px;color:var(--text-muted);';\n  wifiPane.textContent = 'WiFi connection is reserved for the next step.';\n  connectionSection.appendChild(wifiPane);\n\n  function selectConnectionMethod(method) {\n    const usb = method === 'usb';\n    usbPane.style.display = usb ? '' : 'none';\n    wifiPane.style.display = usb ? 'none' : '';\n    usbMethodBtn.style.cssText = usb ? primaryBtnStyle : btnStyle;\n    wifiMethodBtn.style.cssText = usb ? btnStyle : primaryBtnStyle;\n    connectBtn.disabled = !usb;\n  }\n  usbMethodBtn.addEventListener('click', () => selectConnectionMethod('usb'));\n  wifiMethodBtn.addEventListener('click', () => selectConnectionMethod('wifi'));\n  selectConnectionMethod('usb');\n  container.appendChild(connectionSection);\n\n  // Shown only once a port is open but no firmware answered 'ping' --\n  // either a genuinely blank chip, or one running something else entirely.\n  const firmwareSection = document.createElement('div');\n  firmwareSection.style.cssText = sectionStyle + 'display:none;';\n  const firmwareLabel = document.createElement('div');\n  firmwareLabel.textContent = 'NO FIRMWARE DETECTED';\n  firmwareLabel.style.cssText = labelStyle;\n  firmwareSection.appendChild(firmwareLabel);\n  const firmwareRow = document.createElement('div');\n  firmwareRow.style.cssText = 'display:flex;gap:8px;align-items:center;';\n  const firmwareSelect = document.createElement('select');\n  firmwareSelect.style.cssText = 'flex:1;padding:5px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;';\n  const presets = (helpers.serial.firmwarePresets || []).filter((p) => !props.chipFamily || p.chip === props.chipFamily);\n  for (const preset of presets) {\n    const option = document.createElement('option');\n    option.value = preset.id;\n    option.textContent = preset.label;\n    firmwareSelect.appendChild(option);\n  }\n  const installBtn = document.createElement('button');\n  installBtn.type = 'button';\n  installBtn.textContent = 'Install firmware';\n  installBtn.style.cssText = primaryBtnStyle;\n  installBtn.disabled = !presets.length;\n  firmwareRow.append(firmwareSelect, installBtn);\n  firmwareSection.appendChild(firmwareRow);\n  if (!presets.length) {\n    const noPresets = document.createElement('p');\n    noPresets.textContent = 'No firmware published yet for this chip.';\n    noPresets.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n    firmwareSection.appendChild(noPresets);\n  }\n  const progressEl = document.createElement('div');\n  progressEl.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n  firmwareSection.appendChild(progressEl);\n  container.appendChild(firmwareSection);\n\n  // Shown only once the board's own console confirmed it's actually\n  // running -- this is where the \"fixed hardware modules\" live; for now\n  // that's just Digital I/O (see the module's own children), CAN/WiFi etc.\n  // are future, separate submodules, not this one's job.\n  const programSection = document.createElement('div');\n  programSection.style.cssText = sectionStyle + 'display:none;';\n  const programLabel = document.createElement('div');\n  programLabel.textContent = 'DIGITAL I/O';\n  programLabel.style.cssText = labelStyle;\n  programSection.appendChild(programLabel);\n  const programStatus = document.createElement('p');\n  programStatus.style.cssText = 'margin:0 0 8px;font-size:11px;color:var(--text-muted);';\n  programSection.appendChild(programStatus);\n  const sendBtn = document.createElement('button');\n  sendBtn.type = 'button';\n  sendBtn.textContent = 'Save changes to device';\n  sendBtn.style.cssText = btnStyle;\n  programSection.appendChild(sendBtn);\n\n  // Live hardware override -- separate from \"Save changes to device\" above,\n  // which only ever declares pins (see buildMinimalDesign's own doc on why\n  // it can't yet compile real wiring). This talks straight to ioWrite() on\n  // the board (see esp32_logic's `io <gpio> <0|1>` serial command), the same\n  // direct-pin override its own browser circuit editor already does over\n  // WebSocket -- bypasses circuit logic entirely, on both ends.\n  const pinsLabel = document.createElement('div');\n  pinsLabel.textContent = 'LIVE PINS';\n  pinsLabel.style.cssText = labelStyle + 'margin-top:14px;';\n  programSection.appendChild(pinsLabel);\n  const pinsListEl = document.createElement('div');\n  pinsListEl.style.cssText = 'display:flex;flex-direction:column;gap:6px;';\n  programSection.appendChild(pinsListEl);\n  container.appendChild(programSection);\n\n  function updateProgramSection() {\n    const design = helpers.console.buildDevkitDesign ? helpers.console.buildDevkitDesign() : helpers.console.buildMinimalDesign(\n      block.children ? Array.from(block.children.blocks.values()) : [],\n      block.children ? Array.from(block.children.connections.values()) : [],\n    );\n    const summary = helpers.console.designSummary ? helpers.console.designSummary(design) : { blockCount: design.blocks.length, pinCount: 0, timerCount: 0, beltCount: 0 };\n    const snapshot = helpers.console.devkitSnapshot ? helpers.console.devkitSnapshot() : childSnapshot();\n    const lastSent = (block.props.find((p) => p.name === 'lastSentSnapshot') || {}).value || '';\n    const dirty = summary.blockCount && snapshot !== lastSent;\n    programStatus.textContent = !summary.blockCount\n      ? 'No circuit is wired to this ' + (block.name || 'ESP32 DevKit') + ' yet.'\n      : dirty\n        ? 'Ready to send: ' + summary.blockCount + ' block(s), including ' + summary.pinCount + ' pin(s), ' + summary.timerCount + ' timer(s), and ' + summary.beltCount + ' belt(s).'\n        : 'Circuit matches what is on the device.';\n    sendBtn.disabled = !summary.blockCount;\n  }\n  updateProgramSection();\n\n  // One row per Digital I/O child with a real pin set -- the exact same\n  // filter buildMinimalDesign uses, so \"what Save would send\" and \"what\n  // Live Pins shows\" never disagree about which children count.\n  function pinChildren() {\n    const children = block.children ? Array.from(block.children.blocks.values()) : [];\n    return children\n      .filter((c) => (c.props || []).find((p) => p.name === 'noditronKind')?.value === 'digital-io')\n      .map((c) => {\n        const pin = (c.props || []).find((p) => p.name === 'pin')?.value;\n        const direction = (c.props || []).find((p) => p.name === 'direction')?.value === 'output' ? 'output' : 'input';\n        const valueProp = (c.props || []).find((p) => p.name === 'value');\n        return { child: c, pin, direction, valueProp };\n      })\n      .filter((entry) => entry.pin !== null && entry.pin !== undefined && entry.pin !== '');\n  }\n\n  const pinRows = new Map(); // childId -> { row, dot, label, btn }\n\n  async function setChildValue(entry, on) {\n    if (entry.valueProp) entry.valueProp.value = on ? 1 : 0;\n    const bd = await import('/nodigraph/src/model/BlockDescription.js');\n    entry.child.description = bd.serializeBlockDescription(entry.child);\n    helpers.refresh();\n  }\n\n  function renderPinsList() {\n    const entries = pinChildren();\n    const seen = new Set();\n    for (const entry of entries) {\n      seen.add(entry.child.id);\n      let ui = pinRows.get(entry.child.id);\n      if (!ui) {\n        const row = document.createElement('div');\n        row.style.cssText = 'display:flex;align-items:center;gap:8px;font-size:12px;';\n        const dot = document.createElement('span');\n        dot.style.cssText = 'width:10px;height:10px;border-radius:50%;flex:none;border:2px solid var(--border);box-sizing:border-box;';\n        const label = document.createElement('span');\n        label.style.cssText = 'flex:1;color:var(--text-secondary);';\n        const btn = document.createElement('button');\n        btn.type = 'button';\n        btn.style.cssText = btnStyle + 'padding:3px 10px;font-size:11px;';\n        row.append(dot, label, btn);\n        pinsListEl.appendChild(row);\n        ui = { row, dot, label, btn };\n        pinRows.set(entry.child.id, ui);\n      }\n      const on = Number(entry.valueProp && entry.valueProp.value) >= 1;\n      ui.label.textContent = 'GPIO' + entry.pin + ' · ' + entry.direction.toUpperCase() + ' · ' + (entry.child.name || 'Digital I/O');\n      ui.dot.style.background = on ? '#3ecf5d' : 'transparent';\n      ui.dot.style.borderColor = on ? '#3ecf5d' : 'var(--border)';\n      if (entry.direction === 'output') {\n        ui.btn.style.display = '';\n        ui.btn.textContent = on ? 'Set LOW' : 'Set HIGH';\n        ui.btn.onclick = async () => {\n          const next = !on;\n          ui.btn.disabled = true;\n          try {\n            await helpers.console.setPin(entry.pin, next);\n            await setChildValue(entry, next);\n            renderPinsList();\n          } catch (err) {\n            log('Set GPIO' + entry.pin + ' failed: ' + err.message);\n          } finally {\n            ui.btn.disabled = false;\n          }\n        };\n      } else {\n        ui.btn.style.display = 'none';\n        ui.btn.onclick = null;\n      }\n    }\n    for (const [childId, ui] of pinRows) {\n      if (!seen.has(childId)) {\n        ui.row.remove();\n        pinRows.delete(childId);\n      }\n    }\n    pinsLabel.style.display = entries.length ? '' : 'none';\n  }\n  renderPinsList();\n\n  // Reflects hardware-side changes back into the UI (an input pin toggled\n  // by whatever it's wired to on the board, or another client's own live\n  // control) -- not just this dialog's own writes. Stops itself once the\n  // dialog closes (container.isConnected goes false) or the device stops\n  // answering; no explicit \"dialog closed\" hook exists to unsubscribe from\n  // otherwise (see dialogSystem.js's close(), which just clears innerHTML).\n  let pinsPollTimer = null;\n  function startPinsPoll() {\n    if (pinsPollTimer) return;\n    pinsPollTimer = setInterval(async () => {\n      if (!container.isConnected || programSection.style.display === 'none') {\n        clearInterval(pinsPollTimer);\n        pinsPollTimer = null;\n        return;\n      }\n      try {\n        const livePins = await helpers.console.readPins({ timeoutMs: 1500 });\n        const byGpio = new Map(livePins.map((p) => [p.gpio, p]));\n        for (const entry of pinChildren()) {\n          const live = byGpio.get(Number(entry.pin));\n          if (live && entry.valueProp) entry.valueProp.value = live.state ? 1 : 0;\n        }\n        renderPinsList();\n      } catch (_) {\n        // A missed poll just leaves the UI showing the last known state --\n        // no reason to log every dropped byte on a link that otherwise works.\n      }\n    }, 800);\n  }\n\n  // A truly blank chip also needs its own bootloader/partition table, not\n  // just the app image \"Install firmware\" above assumes is already there\n  // (the ordinary case -- factory-fresh or previously-flashed boards both\n  // already have one). Collapsed since most people never need it.\n  const advanced = document.createElement('details');\n  advanced.style.cssText = 'margin:0 0 12px;';\n  const summary = document.createElement('summary');\n  summary.textContent = 'Advanced: flash files manually';\n  summary.style.cssText = 'cursor:pointer;font-size:11px;color:var(--text-muted);margin-bottom:8px;';\n  advanced.appendChild(summary);\n  const rows = document.createElement('div');\n  advanced.appendChild(rows);\n  const addRowBtn = document.createElement('button');\n  addRowBtn.type = 'button';\n  addRowBtn.textContent = '+ Add file';\n  addRowBtn.style.cssText = btnStyle;\n  advanced.appendChild(addRowBtn);\n  const eraseRow = document.createElement('label');\n  eraseRow.style.cssText = 'display:flex;align-items:center;gap:8px;margin:12px 0;cursor:pointer;font-size:12px;';\n  const eraseCheckbox = document.createElement('input');\n  eraseCheckbox.type = 'checkbox';\n  eraseCheckbox.checked = true;\n  eraseRow.append(eraseCheckbox, document.createTextNode('Erase flash before writing (recommended for a blank board)'));\n  advanced.appendChild(eraseRow);\n  const flashBtn = document.createElement('button');\n  flashBtn.type = 'button';\n  flashBtn.textContent = 'Flash';\n  flashBtn.style.cssText = btnStyle;\n  advanced.appendChild(flashBtn);\n  container.appendChild(advanced);\n\n  const logEl = document.createElement('pre');\n  logEl.style.cssText = 'margin:0;padding:8px;border:1px solid var(--border);border-radius:6px;background:var(--bg-well);color:var(--text-secondary);font-family:ui-monospace,Consolas,monospace;font-size:11px;white-space:pre-wrap;word-break:break-word;max-height:140px;overflow:auto;';\n  logEl.textContent = String(props.connectionState || 'disconnected');\n  container.appendChild(logEl);\n  function log(line) {\n    logEl.textContent += '\\n' + line;\n    logEl.scrollTop = logEl.scrollHeight;\n  }\n\n  let bootloaderOffset = null;\n\n  function showRunning() {\n    firmwareSection.style.display = 'none';\n    programSection.style.display = '';\n    updateProgramSection();\n    renderPinsList();\n    startPinsPoll();\n  }\n\n  function showNeedsFirmware() {\n    programSection.style.display = 'none';\n    firmwareSection.style.display = '';\n  }\n\n  // The one place that decides what's actually on the other end of the\n  // port: try the console's own 'ping' first (harmless if it's already\n  // running -- just a status line back), and only reset into the ROM\n  // bootloader if nothing answers. Reused after Connect, and again after\n  // Install firmware once the board's had a moment to boot into it.\n  async function afterPortOpen() {\n    statusEl.textContent = 'Connected -- checking for firmware...';\n    try {\n      const info = await helpers.console.identify({ timeoutMs: 1500 });\n      if (info.verified) {\n        statusEl.textContent = 'Logic Module v' + info.version + ' running.';\n        log('ping: v' + info.version + ' build ' + info.build + ', heap=' + info.heap + '.');\n        helpers.setProp('connectionState', 'connected:running');\n        showRunning();\n        return;\n      }\n    } catch (err) {\n      log('Console check failed: ' + err.message);\n    }\n    try {\n      const detected = await helpers.serial.detectChip(log);\n      bootloaderOffset = detected.bootloaderOffset;\n      statusEl.textContent = detected.chipName + ' detected -- no firmware running yet.';\n      log('Detected ' + detected.chipName + ' (bootloader @ 0x' + detected.bootloaderOffset.toString(16) + ').');\n      helpers.setProp('connectionState', 'connected:needs-firmware:' + detected.chipName);\n      showNeedsFirmware();\n    } catch (err) {\n      statusEl.textContent = 'Connected, but logic firmware was not confirmed.';\n      log('Chip detect failed: ' + err.message);\n      helpers.setProp('connectionState', 'connected:needs-firmware:unknown');\n      showNeedsFirmware();\n    }\n  }\n\n  const existing = helpers.serial.getSession();\n  if (existing) {\n    disconnectBtn.disabled = false;\n    connectBtn.disabled = true;\n    afterPortOpen();\n  } else {\n    statusEl.textContent = 'Not connected.';\n  }\n\n  connectBtn.addEventListener('click', async () => {\n    connectBtn.disabled = true;\n    try {\n      await helpers.serial.connect(log);\n      log('Serial port opened.');\n      disconnectBtn.disabled = false;\n      await afterPortOpen();\n    } catch (err) {\n      statusEl.textContent = 'Connect failed: ' + err.message;\n      connectBtn.disabled = false;\n    }\n  });\n\n  disconnectBtn.addEventListener('click', async () => {\n    helpers.console.close();\n    await helpers.serial.disconnect();\n    statusEl.textContent = 'Disconnected.';\n    connectBtn.disabled = false;\n    disconnectBtn.disabled = true;\n    firmwareSection.style.display = 'none';\n    programSection.style.display = 'none';\n    helpers.setProp('connectionState', 'disconnected');\n  });\n\n  installBtn.addEventListener('click', async () => {\n    const preset = presets.find((p) => p.id === firmwareSelect.value);\n    if (!preset) return;\n    installBtn.disabled = true;\n    const original = installBtn.textContent;\n    installBtn.textContent = 'Installing...';\n    try {\n      const files = await helpers.serial.fetchPresetParts(preset);\n      log('Fetched ' + preset.label + ' (' + files.map((f) => f.name).join(', ') + ').');\n      // Every part of the preset, written together in one writeFlash call --\n      // never just the app image alone. A board that only ever gets its app\n      // partition rewritten keeps whatever bootloader/partition table it\n      // already has, which is fine right up until one doesn't have either\n      // (a genuinely blank or fully erased chip) -- then the ROM loops\n      // \"invalid header: 0xffffffff\" forever with nothing to hand off to.\n      // Rewriting an unchanged bootloader/partition table on a board that\n      // already had one is a harmless no-op, so always including them here\n      // costs nothing on the common case and fixes the blank-chip one.\n      await helpers.serial.flash(\n        files,\n        false,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + '/' + files.length + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      progressEl.textContent = '';\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Install failed: ' + err.message);\n    } finally {\n      installBtn.disabled = false;\n      installBtn.textContent = original;\n    }\n  });\n\n  sendBtn.addEventListener('click', async () => {\n    sendBtn.disabled = true;\n    const original = sendBtn.textContent;\n    sendBtn.textContent = 'Saving...';\n    try {\n      const design = helpers.console.buildDevkitDesign ? helpers.console.buildDevkitDesign() : helpers.console.buildMinimalDesign(\n        block.children ? Array.from(block.children.blocks.values()) : [],\n        block.children ? Array.from(block.children.connections.values()) : [],\n      );\n      if (!design.blocks.length) {\n        log('Nothing to send -- wire a Timer/Bool to a GPIO pin on this ESP32 DevKit first.');\n      } else {\n        await helpers.console.sendDesign(design);\n        const snapshot = helpers.console.devkitSnapshot ? helpers.console.devkitSnapshot() : childSnapshot();\n        if (helpers.console.markDevkitSent) helpers.console.markDevkitSent(snapshot);\n        else helpers.setProp('lastSentSnapshot', snapshot);\n        const summary = helpers.console.designSummary ? helpers.console.designSummary(design) : { blockCount: design.blocks.length, pinCount: design.blocks.filter((b) => b.type === 'din' || b.type === 'dout').length, timerCount: 0, beltCount: 0 };\n        log('Sent ' + summary.blockCount + ' block(s): ' + summary.pinCount + ' pin(s), ' + summary.timerCount + ' timer(s), ' + summary.beltCount + ' belt(s). It applies immediately and survives reboot.');\n        updateProgramSection();\n      }\n    } catch (err) {\n      log('Save failed: ' + err.message);\n    } finally {\n      sendBtn.disabled = false;\n      sendBtn.textContent = original;\n    }\n  });\n\n  function addFileRow() {\n    const row = document.createElement('div');\n    row.style.cssText = 'display:flex;align-items:center;gap:8px;margin-bottom:6px;';\n    const fileInput = document.createElement('input');\n    fileInput.type = 'file';\n    fileInput.accept = '.bin';\n    fileInput.style.cssText = 'flex:1;font-size:11px;min-width:0;';\n    const addrInput = document.createElement('input');\n    addrInput.type = 'text';\n    addrInput.style.cssText = 'width:90px;flex:none;padding:4px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;font-family:ui-monospace,monospace;box-sizing:border-box;';\n    addrInput.value = '0x10000';\n    const removeBtn = document.createElement('button');\n    removeBtn.type = 'button';\n    removeBtn.textContent = String.fromCharCode(215);\n    removeBtn.style.cssText = 'width:22px;height:22px;flex:none;padding:0;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-muted);cursor:pointer;';\n    removeBtn.addEventListener('click', () => row.remove());\n    fileInput.addEventListener('change', () => {\n      const f = fileInput.files[0];\n      if (f) addrInput.value = '0x' + helpers.serial.guessAddress(f.name, bootloaderOffset).toString(16);\n    });\n    row.append(fileInput, addrInput, removeBtn);\n    rows.appendChild(row);\n  }\n  addFileRow();\n  addRowBtn.addEventListener('click', () => addFileRow());\n\n  flashBtn.addEventListener('click', async () => {\n    const files = [];\n    for (const row of rows.children) {\n      const addrInput = row.querySelector('input[type=text]');\n      const address = parseInt(addrInput.value, 16) || 0;\n      const fileInput = row.querySelector('input[type=file]');\n      const file = fileInput && fileInput.files[0];\n      if (!file) continue;\n      files.push({ name: file.name, file, address });\n    }\n    if (!files.length) { log('Pick at least one .bin file first.'); return; }\n    flashBtn.disabled = true;\n    try {\n      await helpers.serial.flash(\n        files,\n        eraseCheckbox.checked,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Flash failed: ' + err.message);\n    } finally {\n      flashBtn.disabled = false;\n    }\n  });\n}\nprop.allowedChildKinds: [\"digital-io\",\"timer\",\"and\",\"or\",\"not\",\"data\",\"add\",\"boot\",\"serial\",\"serialin\",\"can\",\"i2cout\",\"i2cin\",\"pwmout\",\"pwmin\",\"slice\",\"route\",\"croute\"]\nprop.usbOrientation: top\nprop.boardVariant: ESP32 DevKit V1\nprop.pinMap: [{\"label\":\"3V3\",\"gpio\":null,\"role\":\"power\",\"inputOnly\":null,\"side\":\"left\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"GND\",\"gpio\":null,\"role\":\"ground\",\"inputOnly\":null,\"side\":\"left\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"D15\",\"gpio\":15,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"D2\",\"gpio\":2,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"D4\",\"gpio\":4,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"RX2\",\"gpio\":16,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"TX2\",\"gpio\":17,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"D5\",\"gpio\":5,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"D18\",\"gpio\":18,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"D19\",\"gpio\":19,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":9,\"orientation\":\"usb-top\"},{\"label\":\"D21\",\"gpio\":21,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":10,\"orientation\":\"usb-top\"},{\"label\":\"RX0\",\"gpio\":3,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":11,\"orientation\":\"usb-top\"},{\"label\":\"TX0\",\"gpio\":1,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":12,\"orientation\":\"usb-top\"},{\"label\":\"D22\",\"gpio\":22,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":13,\"orientation\":\"usb-top\"},{\"label\":\"D23\",\"gpio\":23,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":14,\"orientation\":\"usb-top\"},{\"label\":\"VIN\",\"gpio\":null,\"role\":\"power\",\"inputOnly\":null,\"side\":\"right\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"GND\",\"gpio\":null,\"role\":\"ground\",\"inputOnly\":null,\"side\":\"right\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"D13\",\"gpio\":13,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"D12\",\"gpio\":12,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"D14\",\"gpio\":14,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"D27\",\"gpio\":27,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"D26\",\"gpio\":26,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"D25\",\"gpio\":25,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"D33\",\"gpio\":33,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"D32\",\"gpio\":32,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":9,\"orientation\":\"usb-top\"},{\"label\":\"D35\",\"gpio\":35,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":10,\"orientation\":\"usb-top\"},{\"label\":\"D34\",\"gpio\":34,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":11,\"orientation\":\"usb-top\"},{\"label\":\"VN\",\"gpio\":39,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":12,\"orientation\":\"usb-top\"},{\"label\":\"VP\",\"gpio\":36,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":13,\"orientation\":\"usb-top\"},{\"label\":\"EN\",\"gpio\":null,\"role\":\"enable\",\"inputOnly\":null,\"side\":\"right\",\"row\":14,\"orientation\":\"usb-top\"}]\nprop.onboardControls: {\"buttons\":[{\"label\":\"GPIO0\",\"gpio\":0,\"role\":\"input\"},{\"label\":\"GPIO35\",\"gpio\":35,\"role\":\"input\"}],\"led\":{\"label\":\"LED\",\"gpio\":2,\"role\":\"output\"}}",
        "geometry": {
          "x": 0,
          "y": 0,
          "width": 300,
          "height": 660
        },
        "style": {
          "color": "#2f6fed",
          "fill": "#ffffff"
        },
        "logicalPorts": [
          {
            "id": "io_esp32_left_3v3_1",
            "name": "3V3",
            "direction": null,
            "description": "power"
          },
          {
            "id": "io_esp32_left_gnd_2",
            "name": "GND",
            "direction": null,
            "description": "ground"
          },
          {
            "id": "io_esp32_left_d15_3",
            "name": "D15",
            "direction": null,
            "description": "GPIO15"
          },
          {
            "id": "io_esp32_left_d2_4",
            "name": "D2",
            "direction": null,
            "description": "GPIO2"
          },
          {
            "id": "io_esp32_left_d4_5",
            "name": "D4",
            "direction": null,
            "description": "GPIO4"
          },
          {
            "id": "io_esp32_left_rx2_6",
            "name": "RX2",
            "direction": null,
            "description": "GPIO16"
          },
          {
            "id": "io_esp32_left_tx2_7",
            "name": "TX2",
            "direction": null,
            "description": "GPIO17"
          },
          {
            "id": "io_esp32_left_d5_8",
            "name": "D5",
            "direction": null,
            "description": "GPIO5"
          },
          {
            "id": "io_esp32_left_d18_9",
            "name": "D18",
            "direction": null,
            "description": "GPIO18"
          },
          {
            "id": "io_esp32_left_d19_10",
            "name": "D19",
            "direction": null,
            "description": "GPIO19"
          },
          {
            "id": "io_esp32_left_d21_11",
            "name": "D21",
            "direction": null,
            "description": "GPIO21"
          },
          {
            "id": "io_esp32_left_rx0_12",
            "name": "RX0",
            "direction": null,
            "description": "GPIO3"
          },
          {
            "id": "io_esp32_left_tx0_13",
            "name": "TX0",
            "direction": null,
            "description": "GPIO1"
          },
          {
            "id": "io_esp32_left_d22_14",
            "name": "D22",
            "direction": null,
            "description": "GPIO22"
          },
          {
            "id": "io_esp32_left_d23_15",
            "name": "D23",
            "direction": null,
            "description": "GPIO23"
          },
          {
            "id": "io_esp32_right_vin_1",
            "name": "VIN",
            "direction": null,
            "description": "power"
          },
          {
            "id": "io_esp32_right_gnd_2",
            "name": "GND",
            "direction": null,
            "description": "ground"
          },
          {
            "id": "io_esp32_right_d13_3",
            "name": "D13",
            "direction": null,
            "description": "GPIO13"
          },
          {
            "id": "io_esp32_right_d12_4",
            "name": "D12",
            "direction": null,
            "description": "GPIO12"
          },
          {
            "id": "io_esp32_right_d14_5",
            "name": "D14",
            "direction": null,
            "description": "GPIO14"
          },
          {
            "id": "io_esp32_right_d27_6",
            "name": "D27",
            "direction": null,
            "description": "GPIO27"
          },
          {
            "id": "io_esp32_right_d26_7",
            "name": "D26",
            "direction": null,
            "description": "GPIO26"
          },
          {
            "id": "io_esp32_right_d25_8",
            "name": "D25",
            "direction": null,
            "description": "GPIO25"
          },
          {
            "id": "io_esp32_right_d33_9",
            "name": "D33",
            "direction": null,
            "description": "GPIO33"
          },
          {
            "id": "io_esp32_right_d32_10",
            "name": "D32",
            "direction": null,
            "description": "GPIO32"
          },
          {
            "id": "io_esp32_right_d35_11",
            "name": "D35",
            "direction": null,
            "description": "GPIO35 input only"
          },
          {
            "id": "io_esp32_right_d34_12",
            "name": "D34",
            "direction": null,
            "description": "GPIO34 input only"
          },
          {
            "id": "io_esp32_right_vn_13",
            "name": "VN",
            "direction": null,
            "description": "GPIO39 input only"
          },
          {
            "id": "io_esp32_right_vp_14",
            "name": "VP",
            "direction": null,
            "description": "GPIO36 input only"
          },
          {
            "id": "io_esp32_right_en_15",
            "name": "EN",
            "direction": null,
            "description": "enable"
          },
          {
            "id": "io_esp32_top_gpio0_boot",
            "name": "GPIO0",
            "direction": null,
            "description": "GPIO0 BOOT button (reads 0 while pressed)"
          },
          {
            "id": "io_esp32_top_usb_serial",
            "name": "USB",
            "direction": "out",
            "description": "USB serial: what the circuit sends here reaches the browser"
          }
        ],
        "ports": [
          {
            "id": "prt_esp32_left_3v3_1",
            "logicalId": "io_esp32_left_3v3_1",
            "side": "left",
            "offset": 30,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_gnd_2",
            "logicalId": "io_esp32_left_gnd_2",
            "side": "left",
            "offset": 70,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d15_3",
            "logicalId": "io_esp32_left_d15_3",
            "side": "left",
            "offset": 110,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d2_4",
            "logicalId": "io_esp32_left_d2_4",
            "side": "left",
            "offset": 150,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d4_5",
            "logicalId": "io_esp32_left_d4_5",
            "side": "left",
            "offset": 190,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_rx2_6",
            "logicalId": "io_esp32_left_rx2_6",
            "side": "left",
            "offset": 230,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_tx2_7",
            "logicalId": "io_esp32_left_tx2_7",
            "side": "left",
            "offset": 270,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d5_8",
            "logicalId": "io_esp32_left_d5_8",
            "side": "left",
            "offset": 310,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d18_9",
            "logicalId": "io_esp32_left_d18_9",
            "side": "left",
            "offset": 350,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d19_10",
            "logicalId": "io_esp32_left_d19_10",
            "side": "left",
            "offset": 390,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d21_11",
            "logicalId": "io_esp32_left_d21_11",
            "side": "left",
            "offset": 430,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_rx0_12",
            "logicalId": "io_esp32_left_rx0_12",
            "side": "left",
            "offset": 470,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_tx0_13",
            "logicalId": "io_esp32_left_tx0_13",
            "side": "left",
            "offset": 510,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d22_14",
            "logicalId": "io_esp32_left_d22_14",
            "side": "left",
            "offset": 550,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_left_d23_15",
            "logicalId": "io_esp32_left_d23_15",
            "side": "left",
            "offset": 590,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_vin_1",
            "logicalId": "io_esp32_right_vin_1",
            "side": "right",
            "offset": 30,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_gnd_2",
            "logicalId": "io_esp32_right_gnd_2",
            "side": "right",
            "offset": 70,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d13_3",
            "logicalId": "io_esp32_right_d13_3",
            "side": "right",
            "offset": 110,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d12_4",
            "logicalId": "io_esp32_right_d12_4",
            "side": "right",
            "offset": 150,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d14_5",
            "logicalId": "io_esp32_right_d14_5",
            "side": "right",
            "offset": 190,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d27_6",
            "logicalId": "io_esp32_right_d27_6",
            "side": "right",
            "offset": 230,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d26_7",
            "logicalId": "io_esp32_right_d26_7",
            "side": "right",
            "offset": 270,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d25_8",
            "logicalId": "io_esp32_right_d25_8",
            "side": "right",
            "offset": 310,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d33_9",
            "logicalId": "io_esp32_right_d33_9",
            "side": "right",
            "offset": 350,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d32_10",
            "logicalId": "io_esp32_right_d32_10",
            "side": "right",
            "offset": 390,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d35_11",
            "logicalId": "io_esp32_right_d35_11",
            "side": "right",
            "offset": 430,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_d34_12",
            "logicalId": "io_esp32_right_d34_12",
            "side": "right",
            "offset": 470,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_vn_13",
            "logicalId": "io_esp32_right_vn_13",
            "side": "right",
            "offset": 510,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_vp_14",
            "logicalId": "io_esp32_right_vp_14",
            "side": "right",
            "offset": 550,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_right_en_15",
            "logicalId": "io_esp32_right_en_15",
            "side": "right",
            "offset": 590,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_top_gpio0_boot",
            "logicalId": "io_esp32_top_gpio0_boot",
            "side": "top",
            "offset": 60,
            "manualOffset": true
          },
          {
            "id": "prt_esp32_top_usb_serial",
            "logicalId": "io_esp32_top_usb_serial",
            "side": "top",
            "offset": 140,
            "manualOffset": true
          }
        ],
        "props": [
          {
            "id": "prp_mtg8v3ep2",
            "name": "noditronKind",
            "kind": "value",
            "value": "esp32-devkit"
          },
          {
            "id": "prp_mtg8v3ep3",
            "name": "chipFamily",
            "kind": "value",
            "value": "ESP32"
          },
          {
            "id": "prp_mtg8v3ep4",
            "name": "connectionState",
            "kind": "value",
            "value": "disconnected"
          },
          {
            "id": "prp_mtg8v3ep5",
            "name": "lastSentSnapshot",
            "kind": "value",
            "value": ""
          },
          {
            "id": "prp_esp32_render",
            "name": "render",
            "kind": "value",
            "value": "const g = block.geometry;\nconst pinMapProp = block.props.find((p) => p.name === 'pinMap');\nlet pins = [];\ntry { pins = JSON.parse(pinMapProp ? pinMapProp.value : '[]'); } catch (_) { pins = []; }\n\nctx.save();\nctx.fillStyle = '#ffffff';\nctx.fillRect(g.x + 12, g.y + 8, g.width - 24, g.height - 16);\n\nctx.fillStyle = '#1f2937';\nctx.font = '20px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textAlign = 'center';\nctx.textBaseline = 'middle';\nctx.fillText(block.name || 'ESP32 DevKit', g.x + g.width / 2, g.y + g.height / 2 + 30, g.width - 48);\n\n// The USB connector sits under the USB pin, wherever that pin is on the\n// top edge (on the slot nodigraph draws it at), like the BOOT button below.\nconst slotOf = (o) => Math.max(20, Math.min(g.width - 20, Math.round((o - 20) / 40) * 40 + 20));\nconst topPinX = (name, fallback) => {\n  const logical = (block.logicalPorts || []).find((lp) => lp.name === name);\n  const pin = logical && (block.ports || []).find((p) => p.logicalId === logical.id && p.side === 'top');\n  return pin ? g.x + slotOf(Number(pin.offset)) : fallback;\n};\nconst usbX = topPinX('USB', g.x + g.width / 2);\nctx.fillStyle = '#e5e7eb';\nctx.strokeStyle = '#9ca3af';\nctx.lineWidth = 1;\nctx.beginPath();\nctx.roundRect(usbX - 36, g.y + 14, 72, 30, 4);\nctx.fill();\nctx.stroke();\nctx.fillStyle = '#6b7280';\nctx.font = '10px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.fillText('USB', usbX, g.y + 29);\n\nconst topY = g.y + 58;\nfunction drawBoardButton(cx, label, fill) {\n  ctx.beginPath();\n  ctx.arc(cx, topY, 13, 0, Math.PI * 2);\n  ctx.fillStyle = fill;\n  ctx.fill();\n  ctx.lineWidth = 1;\n  ctx.strokeStyle = '#94a3b8';\n  ctx.stroke();\n  ctx.fillStyle = '#6b7280';\n  ctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\n  ctx.textAlign = 'center';\n  ctx.fillText(label, cx, topY + 25);\n}\n// The BOOT button sits under the GPIO0 pin, wherever that pin is on the\n// top edge (nodigraph draws a pin on the nearest 40-unit slot: 20, 60, ...).\nconst bootLogical = (block.logicalPorts || []).find((lp) => lp.name === 'GPIO0');\nconst bootPin = bootLogical && (block.ports || []).find((p) => p.logicalId === bootLogical.id && p.side === 'top');\nconst onSlot = (o) => Math.max(20, Math.min(g.width - 20, Math.round((o - 20) / 40) * 40 + 20));\ndrawBoardButton(bootPin ? g.x + onSlot(Number(bootPin.offset)) : g.x + g.width / 2 - 72, 'GPIO0', '#f8fafc');\ndrawBoardButton(g.x + g.width / 2 + 72, 'GPIO35', '#f8fafc');\n\nctx.beginPath();\nctx.arc(g.x + g.width / 2, topY, 7, 0, Math.PI * 2);\nctx.fillStyle = '#3ecf5d';\nctx.fill();\nctx.fillStyle = '#6b7280';\nctx.font = '9px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.fillText('LED GPIO2', g.x + g.width / 2, topY + 25);\n\nctx.font = '15px -apple-system, Segoe UI, Roboto, sans-serif';\nctx.textBaseline = 'middle';\nfor (const pin of pins) {\n  if (pin.side !== 'left' && pin.side !== 'right') continue;\n  const y = g.y + 30 + Number(pin.row || 0) * 40;\n  const left = pin.side === 'left';\n  const labelX = left ? g.x + 26 : g.x + g.width - 26;\n  ctx.fillStyle = pin.role === 'power' ? '#ef4444' : pin.role === 'ground' ? '#6b7280' : pin.inputOnly ? '#64748b' : '#1f2937';\n  ctx.textAlign = left ? 'left' : 'right';\n  ctx.fillText(pin.label, labelX, y, 86);\n}\nctx.restore();"
          },
          {
            "id": "prp_mtg8v3ep6",
            "name": "html",
            "kind": "value",
            "value": "container.style.position = 'absolute';\nlet button = container.querySelector('.esp-connection');\nif (!button) {\n  container.replaceChildren();\n  button = document.createElement('button');\n  button.type = 'button';\n  button.className = 'esp-connection';\n  button.textContent = String.fromCharCode(9881);\n  button.setAttribute('aria-label', 'ESP connection');\n  button.addEventListener('click', helpers.openDialog);\n  container.appendChild(button);\n}\nconst state = block.props.find(p => p.name === 'connectionState')?.value || 'disconnected';\nconst connected = state === 'connected:running';\nconst sent = block.props.find(p => p.name === 'lastSentSnapshot')?.value || '';\nconst dirty = helpers.devkit.snapshot() !== sent;\nconst size = helpers.portSize || 14;\nbutton.style.cssText = 'position:absolute;box-sizing:border-box;padding:0;border:1px solid;border-radius:3px;line-height:1;cursor:pointer;background:var(--bg-well);';\nbutton.style.width = button.style.height = size + 'px';\nbutton.style.top = button.style.right = (4 * (helpers.contentScale || 1)) + 'px';\nbutton.style.fontSize = (size * 0.75) + 'px';\nbutton.style.color = connected ? '#3ecf5d' : '#94a3b8';\nbutton.style.borderColor = connected && dirty ? '#e5b93e' : 'currentColor';\nbutton.title = (connected ? 'Connected' : 'Connect ESP') + (connected && dirty ? ' - circuit not saved' : '') + ' - connection settings';\n"
          },
          {
            "id": "prp_mtg8v3ep7",
            "name": "dialog",
            "kind": "value",
            "value": "container.style.fontFamily = 'Inter, sans-serif';\ncontainer.style.minWidth = '420px';\n\nconst heading = document.createElement('h3');\nheading.textContent = String(block.name || 'ESP32 DEVKIT').toUpperCase();\nheading.style.cssText = 'margin:0 0 14px;color:var(--success,#3ecf5d);font-size:15px;letter-spacing:.03em;';\ncontainer.appendChild(heading);\n\nif (!helpers.serial || !helpers.serial.isSupported()) {\n  const notSupported = document.createElement('p');\n  notSupported.style.cssText = 'margin:0;font-size:12px;color:var(--text-muted);';\n  notSupported.textContent = 'Web Serial is not available in this browser (use Chrome or Edge on desktop).';\n  container.appendChild(notSupported);\n} else {\n  const btnStyle = 'padding:6px 12px;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-primary);font-size:12px;cursor:pointer;';\n  const primaryBtnStyle = 'padding:6px 14px;border:1px solid var(--success,#3ecf5d);border-radius:6px;background:var(--success,#3ecf5d);color:#06210f;font-size:12px;font-weight:600;cursor:pointer;';\n  const sectionStyle = 'margin:0 0 16px;padding-bottom:16px;border-bottom:1px solid var(--border);';\n  const labelStyle = 'font-size:11px;font-weight:700;letter-spacing:.05em;color:var(--text-muted);margin-bottom:8px;';\n\n  function childSnapshot() {\n    if (!block.children) return '';\n    // Connections too, not just the blocks themselves -- rewiring which pin\n    // feeds which (with no other prop change at all) used to leave this\n    // dialog reporting \"Circuit matches what is on the device\" even though\n    // buildMinimalDesign's own belt routing (see serialConsole.js) would\n    // produce a genuinely different design.json for the new wiring.\n    const blocksSnap = Array.from(block.children.blocks.values()).map((c) => ({ id: c.id, props: c.props }));\n    const connsSnap = Array.from(block.children.connections.values()).map((cn) => ({\n      s: cn.sourceBlockId, sp: cn.sourcePortId, t: cn.targetBlockId, tp: cn.targetPortId,\n    }));\n    return JSON.stringify({ blocksSnap, connsSnap });\n  }\n\n  const statusEl = document.createElement('p');\n  statusEl.style.cssText = 'margin:0 0 12px;font-size:12px;color:var(--text-muted);';\n  container.appendChild(statusEl);\n\n  const connectionSection = document.createElement('div');\n  connectionSection.style.cssText = sectionStyle;\n  const connectionLabel = document.createElement('div');\n  connectionLabel.textContent = 'CONNECTION';\n  connectionLabel.style.cssText = labelStyle;\n  connectionSection.appendChild(connectionLabel);\n\n  const methodRow = document.createElement('div');\n  methodRow.style.cssText = 'display:flex;gap:6px;margin-bottom:10px;';\n  const usbMethodBtn = document.createElement('button');\n  usbMethodBtn.type = 'button';\n  usbMethodBtn.textContent = 'USB';\n  usbMethodBtn.style.cssText = primaryBtnStyle;\n  const wifiMethodBtn = document.createElement('button');\n  wifiMethodBtn.type = 'button';\n  wifiMethodBtn.textContent = 'WiFi';\n  wifiMethodBtn.style.cssText = btnStyle;\n  wifiMethodBtn.title = 'WiFi connection will be added later';\n  methodRow.append(usbMethodBtn, wifiMethodBtn);\n  connectionSection.appendChild(methodRow);\n\n  const usbPane = document.createElement('div');\n  const connectRow = document.createElement('div');\n  connectRow.style.cssText = 'display:flex;gap:8px;margin-bottom:8px;';\n  const connectBtn = document.createElement('button');\n  connectBtn.type = 'button';\n  connectBtn.textContent = 'Select serial port';\n  connectBtn.style.cssText = btnStyle;\n  const disconnectBtn = document.createElement('button');\n  disconnectBtn.type = 'button';\n  disconnectBtn.textContent = 'Disconnect';\n  disconnectBtn.style.cssText = btnStyle;\n  disconnectBtn.disabled = true;\n  connectRow.append(connectBtn, disconnectBtn);\n  usbPane.appendChild(connectRow);\n  const usbHint = document.createElement('p');\n  usbHint.style.cssText = 'margin:0;font-size:11px;color:var(--text-muted);';\n  usbHint.textContent = 'Chrome will show the available USB serial ports. Choose the ESP32 DevKit, then Noditron checks whether the logic firmware is already running.';\n  usbPane.appendChild(usbHint);\n  connectionSection.appendChild(usbPane);\n\n  const wifiPane = document.createElement('p');\n  wifiPane.style.cssText = 'display:none;margin:0;font-size:11px;color:var(--text-muted);';\n  wifiPane.textContent = 'WiFi connection is reserved for the next step.';\n  connectionSection.appendChild(wifiPane);\n\n  function selectConnectionMethod(method) {\n    const usb = method === 'usb';\n    usbPane.style.display = usb ? '' : 'none';\n    wifiPane.style.display = usb ? 'none' : '';\n    usbMethodBtn.style.cssText = usb ? primaryBtnStyle : btnStyle;\n    wifiMethodBtn.style.cssText = usb ? btnStyle : primaryBtnStyle;\n    connectBtn.disabled = !usb;\n  }\n  usbMethodBtn.addEventListener('click', () => selectConnectionMethod('usb'));\n  wifiMethodBtn.addEventListener('click', () => selectConnectionMethod('wifi'));\n  selectConnectionMethod('usb');\n  container.appendChild(connectionSection);\n\n  // Shown only once a port is open but no firmware answered 'ping' --\n  // either a genuinely blank chip, or one running something else entirely.\n  const firmwareSection = document.createElement('div');\n  firmwareSection.style.cssText = sectionStyle + 'display:none;';\n  const firmwareLabel = document.createElement('div');\n  firmwareLabel.textContent = 'NO FIRMWARE DETECTED';\n  firmwareLabel.style.cssText = labelStyle;\n  firmwareSection.appendChild(firmwareLabel);\n  const firmwareRow = document.createElement('div');\n  firmwareRow.style.cssText = 'display:flex;gap:8px;align-items:center;';\n  const firmwareSelect = document.createElement('select');\n  firmwareSelect.style.cssText = 'flex:1;padding:5px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;';\n  const presets = (helpers.serial.firmwarePresets || []).filter((p) => !p.board || p.board === props.boardVariant);\n  for (const preset of presets) {\n    const option = document.createElement('option');\n    option.value = preset.id;\n    option.textContent = preset.label;\n    firmwareSelect.appendChild(option);\n  }\n  const preferredPreset = presets.find((p) => p.id === props.firmwarePreset) || presets.find((p) => p.chip === props.chipFamily);\n  if (preferredPreset) firmwareSelect.value = preferredPreset.id;\n  const installBtn = document.createElement('button');\n  installBtn.type = 'button';\n  installBtn.textContent = 'Install firmware';\n  installBtn.style.cssText = primaryBtnStyle;\n  installBtn.disabled = !presets.length;\n  firmwareRow.append(firmwareSelect, installBtn);\n  firmwareSection.appendChild(firmwareRow);\n  if (!presets.length) {\n    const noPresets = document.createElement('p');\n    noPresets.textContent = 'No firmware published yet for this chip.';\n    noPresets.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n    firmwareSection.appendChild(noPresets);\n  }\n  const progressEl = document.createElement('div');\n  progressEl.style.cssText = 'margin:8px 0 0;font-size:11px;color:var(--text-muted);';\n  firmwareSection.appendChild(progressEl);\n  container.appendChild(firmwareSection);\n\n  // Shown only once the board's own console confirmed it's actually\n  // running -- this is where the \"fixed hardware modules\" live; for now\n  // that's just Digital I/O (see the module's own children), CAN/WiFi etc.\n  // are future, separate submodules, not this one's job.\n  const programSection = document.createElement('div');\n  programSection.style.cssText = sectionStyle + 'display:none;';\n  const programLabel = document.createElement('div');\n  programLabel.textContent = 'DIGITAL I/O';\n  programLabel.style.cssText = labelStyle;\n  programSection.appendChild(programLabel);\n  const programStatus = document.createElement('p');\n  programStatus.style.cssText = 'margin:0 0 8px;font-size:11px;color:var(--text-muted);';\n  programSection.appendChild(programStatus);\n  const sendBtn = document.createElement('button');\n  sendBtn.type = 'button';\n  sendBtn.textContent = 'Save changes to device';\n  sendBtn.style.cssText = btnStyle;\n  programSection.appendChild(sendBtn);\n\n  // Live hardware override -- separate from \"Save changes to device\" above,\n  // which only ever declares pins (see buildMinimalDesign's own doc on why\n  // it can't yet compile real wiring). This talks straight to ioWrite() on\n  // the board (see esp32_logic's `io <gpio> <0|1>` serial command), the same\n  // direct-pin override its own browser circuit editor already does over\n  // WebSocket -- bypasses circuit logic entirely, on both ends.\n  const pinsLabel = document.createElement('div');\n  pinsLabel.textContent = 'LIVE PINS';\n  pinsLabel.style.cssText = labelStyle + 'margin-top:14px;';\n  programSection.appendChild(pinsLabel);\n  const pinsListEl = document.createElement('div');\n  pinsListEl.style.cssText = 'display:flex;flex-direction:column;gap:6px;';\n  programSection.appendChild(pinsListEl);\n  container.appendChild(programSection);\n\n  function updateProgramSection() {\n    const design = helpers.console.buildDevkitDesign ? helpers.console.buildDevkitDesign() : helpers.console.buildMinimalDesign(\n      block.children ? Array.from(block.children.blocks.values()) : [],\n      block.children ? Array.from(block.children.connections.values()) : [],\n    );\n    const summary = helpers.console.designSummary ? helpers.console.designSummary(design) : { blockCount: design.blocks.length, pinCount: 0, timerCount: 0, beltCount: 0 };\n    const snapshot = helpers.console.devkitSnapshot ? helpers.console.devkitSnapshot() : childSnapshot();\n    const lastSent = (block.props.find((p) => p.name === 'lastSentSnapshot') || {}).value || '';\n    const dirty = summary.blockCount && snapshot !== lastSent;\n    programStatus.textContent = !summary.blockCount\n      ? 'No circuit is wired to this ' + (block.name || 'ESP32 DevKit') + ' yet.'\n      : dirty\n        ? 'Ready to send: ' + summary.blockCount + ' block(s), including ' + summary.pinCount + ' pin(s), ' + summary.timerCount + ' timer(s), and ' + summary.beltCount + ' belt(s).'\n        : 'Circuit matches what is on the device.';\n    sendBtn.disabled = !summary.blockCount;\n  }\n  updateProgramSection();\n\n  // One row per Digital I/O child with a real pin set -- the exact same\n  // filter buildMinimalDesign uses, so \"what Save would send\" and \"what\n  // Live Pins shows\" never disagree about which children count.\n  function pinChildren() {\n    const children = block.children ? Array.from(block.children.blocks.values()) : [];\n    return children\n      .filter((c) => (c.props || []).find((p) => p.name === 'noditronKind')?.value === 'digital-io')\n      .map((c) => {\n        const pin = (c.props || []).find((p) => p.name === 'pin')?.value;\n        const direction = (c.props || []).find((p) => p.name === 'direction')?.value === 'output' ? 'output' : 'input';\n        const valueProp = (c.props || []).find((p) => p.name === 'value');\n        return { child: c, pin, direction, valueProp };\n      })\n      .filter((entry) => entry.pin !== null && entry.pin !== undefined && entry.pin !== '');\n  }\n\n  const pinRows = new Map(); // childId -> { row, dot, label, btn }\n\n  async function setChildValue(entry, on) {\n    if (entry.valueProp) entry.valueProp.value = on ? 1 : 0;\n    const bd = await import('/nodigraph/src/model/BlockDescription.js');\n    entry.child.description = bd.serializeBlockDescription(entry.child);\n    helpers.refresh();\n  }\n\n  function renderPinsList() {\n    const entries = pinChildren();\n    const seen = new Set();\n    for (const entry of entries) {\n      seen.add(entry.child.id);\n      let ui = pinRows.get(entry.child.id);\n      if (!ui) {\n        const row = document.createElement('div');\n        row.style.cssText = 'display:flex;align-items:center;gap:8px;font-size:12px;';\n        const dot = document.createElement('span');\n        dot.style.cssText = 'width:10px;height:10px;border-radius:50%;flex:none;border:2px solid var(--border);box-sizing:border-box;';\n        const label = document.createElement('span');\n        label.style.cssText = 'flex:1;color:var(--text-secondary);';\n        const btn = document.createElement('button');\n        btn.type = 'button';\n        btn.style.cssText = btnStyle + 'padding:3px 10px;font-size:11px;';\n        row.append(dot, label, btn);\n        pinsListEl.appendChild(row);\n        ui = { row, dot, label, btn };\n        pinRows.set(entry.child.id, ui);\n      }\n      const on = Number(entry.valueProp && entry.valueProp.value) >= 1;\n      ui.label.textContent = 'GPIO' + entry.pin + ' · ' + entry.direction.toUpperCase() + ' · ' + (entry.child.name || 'Digital I/O');\n      ui.dot.style.background = on ? '#3ecf5d' : 'transparent';\n      ui.dot.style.borderColor = on ? '#3ecf5d' : 'var(--border)';\n      if (entry.direction === 'output') {\n        ui.btn.style.display = '';\n        ui.btn.textContent = on ? 'Set LOW' : 'Set HIGH';\n        ui.btn.onclick = async () => {\n          const next = !on;\n          ui.btn.disabled = true;\n          try {\n            await helpers.console.setPin(entry.pin, next);\n            await setChildValue(entry, next);\n            renderPinsList();\n          } catch (err) {\n            log('Set GPIO' + entry.pin + ' failed: ' + err.message);\n          } finally {\n            ui.btn.disabled = false;\n          }\n        };\n      } else {\n        ui.btn.style.display = 'none';\n        ui.btn.onclick = null;\n      }\n    }\n    for (const [childId, ui] of pinRows) {\n      if (!seen.has(childId)) {\n        ui.row.remove();\n        pinRows.delete(childId);\n      }\n    }\n    pinsLabel.style.display = entries.length ? '' : 'none';\n  }\n  renderPinsList();\n\n  // Reflects hardware-side changes back into the UI (an input pin toggled\n  // by whatever it's wired to on the board, or another client's own live\n  // control) -- not just this dialog's own writes. Stops itself once the\n  // dialog closes (container.isConnected goes false) or the device stops\n  // answering; no explicit \"dialog closed\" hook exists to unsubscribe from\n  // otherwise (see dialogSystem.js's close(), which just clears innerHTML).\n  let pinsPollTimer = null;\n  function startPinsPoll() {\n    if (pinsPollTimer) return;\n    pinsPollTimer = setInterval(async () => {\n      if (!container.isConnected || programSection.style.display === 'none') {\n        clearInterval(pinsPollTimer);\n        pinsPollTimer = null;\n        return;\n      }\n      try {\n        const livePins = await helpers.console.readPins({ timeoutMs: 1500 });\n        const byGpio = new Map(livePins.map((p) => [p.gpio, p]));\n        for (const entry of pinChildren()) {\n          const live = byGpio.get(Number(entry.pin));\n          if (live && entry.valueProp) entry.valueProp.value = live.state ? 1 : 0;\n        }\n        renderPinsList();\n      } catch (_) {\n        // A missed poll just leaves the UI showing the last known state --\n        // no reason to log every dropped byte on a link that otherwise works.\n      }\n    }, 800);\n  }\n\n  // A truly blank chip also needs its own bootloader/partition table, not\n  // just the app image \"Install firmware\" above assumes is already there\n  // (the ordinary case -- factory-fresh or previously-flashed boards both\n  // already have one). Collapsed since most people never need it.\n  const advanced = document.createElement('details');\n  advanced.style.cssText = 'margin:0 0 12px;';\n  const summary = document.createElement('summary');\n  summary.textContent = 'Advanced: flash files manually';\n  summary.style.cssText = 'cursor:pointer;font-size:11px;color:var(--text-muted);margin-bottom:8px;';\n  advanced.appendChild(summary);\n  const rows = document.createElement('div');\n  advanced.appendChild(rows);\n  const addRowBtn = document.createElement('button');\n  addRowBtn.type = 'button';\n  addRowBtn.textContent = '+ Add file';\n  addRowBtn.style.cssText = btnStyle;\n  advanced.appendChild(addRowBtn);\n  const eraseRow = document.createElement('label');\n  eraseRow.style.cssText = 'display:flex;align-items:center;gap:8px;margin:12px 0;cursor:pointer;font-size:12px;';\n  const eraseCheckbox = document.createElement('input');\n  eraseCheckbox.type = 'checkbox';\n  eraseCheckbox.checked = true;\n  eraseRow.append(eraseCheckbox, document.createTextNode('Erase flash before writing (recommended for a blank board)'));\n  advanced.appendChild(eraseRow);\n  const flashBtn = document.createElement('button');\n  flashBtn.type = 'button';\n  flashBtn.textContent = 'Flash';\n  flashBtn.style.cssText = btnStyle;\n  advanced.appendChild(flashBtn);\n  container.appendChild(advanced);\n\n  const logEl = document.createElement('pre');\n  logEl.style.cssText = 'margin:0;padding:8px;border:1px solid var(--border);border-radius:6px;background:var(--bg-well);color:var(--text-secondary);font-family:ui-monospace,Consolas,monospace;font-size:11px;white-space:pre-wrap;word-break:break-word;max-height:140px;overflow:auto;';\n  logEl.textContent = String(props.connectionState || 'disconnected');\n  container.appendChild(logEl);\n  function log(line) {\n    logEl.textContent += '\\n' + line;\n    logEl.scrollTop = logEl.scrollHeight;\n  }\n\n  let bootloaderOffset = null;\n\n  function showRunning(info) {\n    const updateAvailable = Boolean(preferredPreset && preferredPreset.build && info && info.build && info.build < preferredPreset.build);\n    firmwareSection.style.display = updateAvailable ? '' : 'none';\n    if (updateAvailable) {\n      firmwareLabel.textContent = 'FIRMWARE UPDATE AVAILABLE';\n      installBtn.textContent = 'Update firmware';\n      progressEl.textContent = 'Installed build ' + info.build + '; available build ' + preferredPreset.build + '.';\n    }\n    programSection.style.display = '';\n    updateProgramSection();\n    renderPinsList();\n    startPinsPoll();\n  }\n\n  function showNeedsFirmware() {\n    programSection.style.display = 'none';\n    firmwareSection.style.display = '';\n  }\n\n  // The one place that decides what's actually on the other end of the\n  // port: try the console's own 'ping' first (harmless if it's already\n  // running -- just a status line back), and only reset into the ROM\n  // bootloader if nothing answers. Reused after Connect, and again after\n  // Install firmware once the board's had a moment to boot into it.\n  async function afterPortOpen() {\n    statusEl.textContent = 'Connected -- checking for firmware...';\n    try {\n      const info = await helpers.console.identify();\n      if (info.verified) {\n        statusEl.textContent = 'Logic Module v' + info.version + ' build ' + info.build + ' running.';\n        log('ping: v' + info.version + ' build ' + info.build + ', heap=' + info.heap + '.');\n        helpers.setProp('connectionState', 'connected:running');\n        showRunning(info);\n        return;\n      }\n    } catch (err) {\n      log('Console check failed: ' + err.message);\n    }\n    try {\n      const detected = await helpers.serial.detectChip(log);\n      bootloaderOffset = detected.bootloaderOffset;\n      const family = detected.chipName.split(' ')[0];\n      const matchingPreset = presets.find((p) => p.chip === family && p.id === props.firmwarePreset) || presets.find((p) => p.chip === family);\n      firmwareSelect.value = matchingPreset ? matchingPreset.id : '';\n      for (const option of firmwareSelect.options) {\n        option.disabled = !presets.some((p) => p.id === option.value && p.chip === family);\n      }\n      installBtn.disabled = !matchingPreset;\n      log('Detected ' + detected.chipName + ' (bootloader @ 0x' + detected.bootloaderOffset.toString(16) + ').');\n      // Detecting the chip is not free: it drives the board into its ROM\n      // bootloader and uploads esptool's stub, and nothing there ever hands\n      // control back to the app. So by this point the board is not running\n      // whatever it has, and asking again while it sits like that would\n      // answer a question about our own doing rather than about the board.\n      // identify() resets it back out (see serialFlash's reopenPlain) and\n      // asks once more -- a board that was running all along says so here,\n      // instead of being reported as blank because we stopped it.\n      try {\n        const after = await helpers.console.identify();\n        if (after.verified) {\n          statusEl.textContent = 'Logic Module v' + after.version + ' build ' + after.build + ' running.';\n          log('ping: v' + after.version + ' build ' + after.build + ', heap=' + after.heap + '.');\n          helpers.setProp('connectionState', 'connected:running');\n          showRunning(after);\n          return;\n        }\n      } catch (err) {\n        log('Console re-check failed: ' + err.message);\n      }\n      statusEl.textContent = detected.chipName + ' detected -- logic firmware did not respond.';\n      helpers.setProp('connectionState', 'connected:needs-firmware:' + detected.chipName);\n      showNeedsFirmware();\n    } catch (err) {\n      statusEl.textContent = 'Connected, but logic firmware was not confirmed.';\n      log('Chip detect failed: ' + err.message);\n      helpers.setProp('connectionState', 'connected:needs-firmware:unknown');\n      showNeedsFirmware();\n    }\n  }\n\n  const existing = helpers.serial.getSession();\n  if (existing) {\n    disconnectBtn.disabled = false;\n    connectBtn.disabled = true;\n    afterPortOpen();\n  } else {\n    statusEl.textContent = 'Not connected.';\n  }\n\n  connectBtn.addEventListener('click', async () => {\n    connectBtn.disabled = true;\n    try {\n      await helpers.serial.connect(log);\n      log('Serial port opened.');\n      disconnectBtn.disabled = false;\n      await afterPortOpen();\n    } catch (err) {\n      statusEl.textContent = 'Connect failed: ' + err.message;\n      connectBtn.disabled = false;\n    }\n  });\n\n  disconnectBtn.addEventListener('click', async () => {\n    helpers.console.close();\n    await helpers.serial.disconnect();\n    statusEl.textContent = 'Disconnected.';\n    connectBtn.disabled = false;\n    disconnectBtn.disabled = true;\n    firmwareSection.style.display = 'none';\n    programSection.style.display = 'none';\n    helpers.setProp('connectionState', 'disconnected');\n  });\n\n  installBtn.addEventListener('click', async () => {\n    const preset = presets.find((p) => p.id === firmwareSelect.value);\n    if (!preset) return;\n    installBtn.disabled = true;\n    const original = installBtn.textContent;\n    installBtn.textContent = 'Installing...';\n    try {\n      const files = await helpers.serial.fetchPresetParts(preset);\n      log('Fetched ' + preset.label + ' (' + files.map((f) => f.name).join(', ') + ').');\n      // Every part of the preset, written together in one writeFlash call --\n      // never just the app image alone. A board that only ever gets its app\n      // partition rewritten keeps whatever bootloader/partition table it\n      // already has, which is fine right up until one doesn't have either\n      // (a genuinely blank or fully erased chip) -- then the ROM loops\n      // \"invalid header: 0xffffffff\" forever with nothing to hand off to.\n      // Rewriting an unchanged bootloader/partition table on a board that\n      // already had one is a harmless no-op, so always including them here\n      // costs nothing on the common case and fixes the blank-chip one.\n      await helpers.serial.flash(\n        files,\n        false,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + '/' + files.length + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      progressEl.textContent = '';\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Install failed: ' + err.message);\n    } finally {\n      installBtn.disabled = false;\n      installBtn.textContent = original;\n    }\n  });\n\n  sendBtn.addEventListener('click', async () => {\n    sendBtn.disabled = true;\n    const original = sendBtn.textContent;\n    sendBtn.textContent = 'Saving...';\n    try {\n      const design = helpers.console.buildDevkitDesign ? helpers.console.buildDevkitDesign() : helpers.console.buildMinimalDesign(\n        block.children ? Array.from(block.children.blocks.values()) : [],\n        block.children ? Array.from(block.children.connections.values()) : [],\n      );\n      if (!design.blocks.length) {\n        log('Nothing to send -- wire a Timer/Bool to a GPIO pin on this ESP32 DevKit first.');\n      } else {\n        await helpers.console.sendDesign(design);\n        const snapshot = helpers.console.devkitSnapshot ? helpers.console.devkitSnapshot() : childSnapshot();\n        if (helpers.console.markDevkitSent) helpers.console.markDevkitSent(snapshot);\n        else helpers.setProp('lastSentSnapshot', snapshot);\n        const summary = helpers.console.designSummary ? helpers.console.designSummary(design) : { blockCount: design.blocks.length, pinCount: design.blocks.filter((b) => b.type === 'din' || b.type === 'dout').length, timerCount: 0, beltCount: 0 };\n        log('Sent ' + summary.blockCount + ' block(s): ' + summary.pinCount + ' pin(s), ' + summary.timerCount + ' timer(s), ' + summary.beltCount + ' belt(s). It applies immediately and survives reboot.');\n        updateProgramSection();\n      }\n    } catch (err) {\n      log('Save failed: ' + err.message);\n    } finally {\n      sendBtn.disabled = false;\n      sendBtn.textContent = original;\n    }\n  });\n\n  function addFileRow() {\n    const row = document.createElement('div');\n    row.style.cssText = 'display:flex;align-items:center;gap:8px;margin-bottom:6px;';\n    const fileInput = document.createElement('input');\n    fileInput.type = 'file';\n    fileInput.accept = '.bin';\n    fileInput.style.cssText = 'flex:1;font-size:11px;min-width:0;';\n    const addrInput = document.createElement('input');\n    addrInput.type = 'text';\n    addrInput.style.cssText = 'width:90px;flex:none;padding:4px 6px;background:none;border:1px solid var(--border);border-radius:6px;color:var(--text-primary);font-size:12px;font-family:ui-monospace,monospace;box-sizing:border-box;';\n    addrInput.value = '0x10000';\n    const removeBtn = document.createElement('button');\n    removeBtn.type = 'button';\n    removeBtn.textContent = String.fromCharCode(215);\n    removeBtn.style.cssText = 'width:22px;height:22px;flex:none;padding:0;border:1px solid var(--border);border-radius:6px;background:none;color:var(--text-muted);cursor:pointer;';\n    removeBtn.addEventListener('click', () => row.remove());\n    fileInput.addEventListener('change', () => {\n      const f = fileInput.files[0];\n      if (f) addrInput.value = '0x' + helpers.serial.guessAddress(f.name, bootloaderOffset).toString(16);\n    });\n    row.append(fileInput, addrInput, removeBtn);\n    rows.appendChild(row);\n  }\n  addFileRow();\n  addRowBtn.addEventListener('click', () => addFileRow());\n\n  flashBtn.addEventListener('click', async () => {\n    const files = [];\n    for (const row of rows.children) {\n      const addrInput = row.querySelector('input[type=text]');\n      const address = parseInt(addrInput.value, 16) || 0;\n      const fileInput = row.querySelector('input[type=file]');\n      const file = fileInput && fileInput.files[0];\n      if (!file) continue;\n      files.push({ name: file.name, file, address });\n    }\n    if (!files.length) { log('Pick at least one .bin file first.'); return; }\n    flashBtn.disabled = true;\n    try {\n      await helpers.serial.flash(\n        files,\n        eraseCheckbox.checked,\n        (fileIndex, written, total) => {\n          progressEl.textContent = 'File ' + (fileIndex + 1) + ': ' + Math.round((written / total) * 100) + '%';\n        },\n        log,\n      );\n      statusEl.textContent = 'Flashed -- waiting for the board to boot...';\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n      await afterPortOpen();\n    } catch (err) {\n      log('Flash failed: ' + err.message);\n    } finally {\n      flashBtn.disabled = false;\n    }\n  });\n}"
          },
          {
            "id": "prp_mtg8v3ep8",
            "name": "allowedChildKinds",
            "kind": "value",
            "value": "[\"digital-io\",\"timer\",\"and\",\"or\",\"not\",\"data\",\"add\",\"boot\",\"serial\",\"serialin\",\"can\",\"i2cout\",\"i2cin\",\"pwmout\",\"pwmin\",\"slice\",\"route\",\"croute\"]"
          },
          {
            "id": "prp_esp32_usbOrientation",
            "name": "usbOrientation",
            "kind": "value",
            "value": "top"
          },
          {
            "id": "prp_esp32_boardVariant",
            "name": "boardVariant",
            "kind": "value",
            "value": "ESP32 DevKit V1"
          },
          {
            "id": "prp_esp32_pinMap",
            "name": "pinMap",
            "kind": "value",
            "value": "[{\"label\":\"3V3\",\"gpio\":null,\"role\":\"power\",\"inputOnly\":null,\"side\":\"left\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"GND\",\"gpio\":null,\"role\":\"ground\",\"inputOnly\":null,\"side\":\"left\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"D15\",\"gpio\":15,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"D2\",\"gpio\":2,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"D4\",\"gpio\":4,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"RX2\",\"gpio\":16,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"TX2\",\"gpio\":17,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"D5\",\"gpio\":5,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"D18\",\"gpio\":18,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"D19\",\"gpio\":19,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":9,\"orientation\":\"usb-top\"},{\"label\":\"D21\",\"gpio\":21,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":10,\"orientation\":\"usb-top\"},{\"label\":\"RX0\",\"gpio\":3,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":11,\"orientation\":\"usb-top\"},{\"label\":\"TX0\",\"gpio\":1,\"role\":\"uart\",\"inputOnly\":false,\"side\":\"left\",\"row\":12,\"orientation\":\"usb-top\"},{\"label\":\"D22\",\"gpio\":22,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":13,\"orientation\":\"usb-top\"},{\"label\":\"D23\",\"gpio\":23,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"left\",\"row\":14,\"orientation\":\"usb-top\"},{\"label\":\"VIN\",\"gpio\":null,\"role\":\"power\",\"inputOnly\":null,\"side\":\"right\",\"row\":0,\"orientation\":\"usb-top\"},{\"label\":\"GND\",\"gpio\":null,\"role\":\"ground\",\"inputOnly\":null,\"side\":\"right\",\"row\":1,\"orientation\":\"usb-top\"},{\"label\":\"D13\",\"gpio\":13,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":2,\"orientation\":\"usb-top\"},{\"label\":\"D12\",\"gpio\":12,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":3,\"orientation\":\"usb-top\"},{\"label\":\"D14\",\"gpio\":14,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":4,\"orientation\":\"usb-top\"},{\"label\":\"D27\",\"gpio\":27,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":5,\"orientation\":\"usb-top\"},{\"label\":\"D26\",\"gpio\":26,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":6,\"orientation\":\"usb-top\"},{\"label\":\"D25\",\"gpio\":25,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":7,\"orientation\":\"usb-top\"},{\"label\":\"D33\",\"gpio\":33,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":8,\"orientation\":\"usb-top\"},{\"label\":\"D32\",\"gpio\":32,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"right\",\"row\":9,\"orientation\":\"usb-top\"},{\"label\":\"D35\",\"gpio\":35,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":10,\"orientation\":\"usb-top\"},{\"label\":\"D34\",\"gpio\":34,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":11,\"orientation\":\"usb-top\"},{\"label\":\"VN\",\"gpio\":39,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":12,\"orientation\":\"usb-top\"},{\"label\":\"VP\",\"gpio\":36,\"role\":\"gpio\",\"inputOnly\":true,\"side\":\"right\",\"row\":13,\"orientation\":\"usb-top\"},{\"label\":\"EN\",\"gpio\":null,\"role\":\"enable\",\"inputOnly\":null,\"side\":\"right\",\"row\":14,\"orientation\":\"usb-top\"},{\"label\":\"GPIO0\",\"gpio\":0,\"role\":\"gpio\",\"inputOnly\":false,\"side\":\"top\",\"row\":null,\"orientation\":\"usb-top\",\"onboard\":\"boot-button\"},{\"label\":\"USB\",\"gpio\":null,\"role\":\"usb-serial\",\"inputOnly\":null,\"side\":\"top\",\"row\":null,\"orientation\":\"usb-top\"}]"
          },
          {
            "id": "prp_esp32_onboardControls",
            "name": "onboardControls",
            "kind": "value",
            "value": "{\"buttons\":[{\"label\":\"GPIO0\",\"gpio\":0,\"role\":\"input\"},{\"label\":\"GPIO35\",\"gpio\":35,\"role\":\"input\"}],\"led\":{\"label\":\"LED\",\"gpio\":2,\"role\":\"output\"}}"
          }
        ],
        "hasChildren": true,
        "boundaryGeometry": {
          "x": 0,
          "y": 0,
          "width": 300,
          "height": 660
        },
        "requirementIds": []
      }
    ],
    "connections": []
  }
}
